module.exports = {

"[next]/internal/font/google/sora_177802fc.module.css [app-rsc] (css module)": (({ r: __turbopack_require__, f: __turbopack_module_context__, i: __turbopack_import__, s: __turbopack_esm__, v: __turbopack_export_value__, n: __turbopack_export_namespace__, c: __turbopack_cache__, M: __turbopack_modules__, l: __turbopack_load__, j: __turbopack_dynamic__, P: __turbopack_resolve_absolute_path__, U: __turbopack_relative_url__, R: __turbopack_resolve_module_id_path__, g: global, __dirname }) => (() => {

__turbopack_export_value__({
  "className": "sora_177802fc-module__1KITWG__className",
  "variable": "sora_177802fc-module__1KITWG__variable",
});

})()),
"[next]/internal/font/google/sora_177802fc.js [app-rsc] (ecmascript)": (({ r: __turbopack_require__, f: __turbopack_module_context__, i: __turbopack_import__, s: __turbopack_esm__, v: __turbopack_export_value__, n: __turbopack_export_namespace__, c: __turbopack_cache__, M: __turbopack_modules__, l: __turbopack_load__, j: __turbopack_dynamic__, P: __turbopack_resolve_absolute_path__, U: __turbopack_relative_url__, R: __turbopack_resolve_module_id_path__, g: global, __dirname, x: __turbopack_external_require__, y: __turbopack_external_import__ }) => (() => {
"use strict";

__turbopack_esm__({
    "default": ()=>__TURBOPACK__default__export__
});
var __TURBOPACK__imported__module__$5b$next$5d2f$internal$2f$font$2f$google$2f$sora_177802fc$2e$module$2e$css__$5b$app$2d$rsc$5d$__$28$css__module$29$__ = __turbopack_import__("[next]/internal/font/google/sora_177802fc.module.css [app-rsc] (css module)");
"__TURBOPACK__ecmascript__hoisting__location__";
;
const fontData = {
    className: __TURBOPACK__imported__module__$5b$next$5d2f$internal$2f$font$2f$google$2f$sora_177802fc$2e$module$2e$css__$5b$app$2d$rsc$5d$__$28$css__module$29$__["default"].className,
    style: {
        fontFamily: "'__Sora_177802', '__Sora_Fallback_177802'",
        fontStyle: "normal"
    }
};
if (__TURBOPACK__imported__module__$5b$next$5d2f$internal$2f$font$2f$google$2f$sora_177802fc$2e$module$2e$css__$5b$app$2d$rsc$5d$__$28$css__module$29$__["default"].variable != null) {
    fontData.variable = __TURBOPACK__imported__module__$5b$next$5d2f$internal$2f$font$2f$google$2f$sora_177802fc$2e$module$2e$css__$5b$app$2d$rsc$5d$__$28$css__module$29$__["default"].variable;
}
const __TURBOPACK__default__export__ = fontData;

})()),
"[next]/internal/font/google/merriweather_ee88fc3.module.css [app-rsc] (css module)": (({ r: __turbopack_require__, f: __turbopack_module_context__, i: __turbopack_import__, s: __turbopack_esm__, v: __turbopack_export_value__, n: __turbopack_export_namespace__, c: __turbopack_cache__, M: __turbopack_modules__, l: __turbopack_load__, j: __turbopack_dynamic__, P: __turbopack_resolve_absolute_path__, U: __turbopack_relative_url__, R: __turbopack_resolve_module_id_path__, g: global, __dirname }) => (() => {

__turbopack_export_value__({
  "className": "merriweather_ee88fc3-module__gQpU9a__className",
  "variable": "merriweather_ee88fc3-module__gQpU9a__variable",
});

})()),
"[next]/internal/font/google/merriweather_ee88fc3.js [app-rsc] (ecmascript)": (({ r: __turbopack_require__, f: __turbopack_module_context__, i: __turbopack_import__, s: __turbopack_esm__, v: __turbopack_export_value__, n: __turbopack_export_namespace__, c: __turbopack_cache__, M: __turbopack_modules__, l: __turbopack_load__, j: __turbopack_dynamic__, P: __turbopack_resolve_absolute_path__, U: __turbopack_relative_url__, R: __turbopack_resolve_module_id_path__, g: global, __dirname, x: __turbopack_external_require__, y: __turbopack_external_import__ }) => (() => {
"use strict";

__turbopack_esm__({
    "default": ()=>__TURBOPACK__default__export__
});
var __TURBOPACK__imported__module__$5b$next$5d2f$internal$2f$font$2f$google$2f$merriweather_ee88fc3$2e$module$2e$css__$5b$app$2d$rsc$5d$__$28$css__module$29$__ = __turbopack_import__("[next]/internal/font/google/merriweather_ee88fc3.module.css [app-rsc] (css module)");
"__TURBOPACK__ecmascript__hoisting__location__";
;
const fontData = {
    className: __TURBOPACK__imported__module__$5b$next$5d2f$internal$2f$font$2f$google$2f$merriweather_ee88fc3$2e$module$2e$css__$5b$app$2d$rsc$5d$__$28$css__module$29$__["default"].className,
    style: {
        fontFamily: "'__Merriweather_ee88fc', '__Merriweather_Fallback_ee88fc'",
        fontStyle: "normal"
    }
};
if (__TURBOPACK__imported__module__$5b$next$5d2f$internal$2f$font$2f$google$2f$merriweather_ee88fc3$2e$module$2e$css__$5b$app$2d$rsc$5d$__$28$css__module$29$__["default"].variable != null) {
    fontData.variable = __TURBOPACK__imported__module__$5b$next$5d2f$internal$2f$font$2f$google$2f$merriweather_ee88fc3$2e$module$2e$css__$5b$app$2d$rsc$5d$__$28$css__module$29$__["default"].variable;
}
const __TURBOPACK__default__export__ = fontData;

})()),
"[project]/src/components/ui/toaster.tsx (client proxy)": (({ r: __turbopack_require__, f: __turbopack_module_context__, i: __turbopack_import__, s: __turbopack_esm__, v: __turbopack_export_value__, n: __turbopack_export_namespace__, c: __turbopack_cache__, M: __turbopack_modules__, l: __turbopack_load__, j: __turbopack_dynamic__, P: __turbopack_resolve_absolute_path__, U: __turbopack_relative_url__, R: __turbopack_resolve_module_id_path__, g: global, __dirname, x: __turbopack_external_require__, y: __turbopack_external_import__ }) => (() => {
"use strict";

__turbopack_esm__({
    "Toaster": ()=>Toaster
});
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$server$2f$future$2f$route$2d$modules$2f$app$2d$page$2f$vendored$2f$rsc$2f$react$2d$server$2d$dom$2d$turbopack$2d$server$2d$edge$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__ = __turbopack_import__("[project]/node_modules/next/dist/server/future/route-modules/app-page/vendored/rsc/react-server-dom-turbopack-server-edge.js [app-rsc] (ecmascript)");
"__TURBOPACK__ecmascript__hoisting__location__";
;
const Toaster = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$server$2f$future$2f$route$2d$modules$2f$app$2d$page$2f$vendored$2f$rsc$2f$react$2d$server$2d$dom$2d$turbopack$2d$server$2d$edge$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["registerClientReference"])(function() {
    throw new Error("Attempted to call Toaster() from the server but Toaster is on the client. It's not possible to invoke a client function from the server, it can only be rendered as a Component or passed to props of a Client Component.");
}, "[project]/src/components/ui/toaster.tsx", "Toaster");

})()),
"[project]/src/components/ui/toaster.tsx [app-rsc] (ecmascript)": (({ r: __turbopack_require__, f: __turbopack_module_context__, i: __turbopack_import__, s: __turbopack_esm__, v: __turbopack_export_value__, n: __turbopack_export_namespace__, c: __turbopack_cache__, M: __turbopack_modules__, l: __turbopack_load__, j: __turbopack_dynamic__, P: __turbopack_resolve_absolute_path__, U: __turbopack_relative_url__, R: __turbopack_resolve_module_id_path__, g: global, __dirname, x: __turbopack_external_require__, y: __turbopack_external_import__ }) => (() => {
"use strict";

var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$components$2f$ui$2f$toaster$2e$tsx__$28$client__proxy$29$__ = __turbopack_import__("[project]/src/components/ui/toaster.tsx (client proxy)");
"__TURBOPACK__ecmascript__hoisting__location__";
"TURBOPACK { transition: next-ecmascript-client-reference }";
;
__turbopack_export_namespace__(__TURBOPACK__imported__module__$5b$project$5d2f$src$2f$components$2f$ui$2f$toaster$2e$tsx__$28$client__proxy$29$__);

})()),
"[project]/src/context/auth-context.tsx (client proxy)": (({ r: __turbopack_require__, f: __turbopack_module_context__, i: __turbopack_import__, s: __turbopack_esm__, v: __turbopack_export_value__, n: __turbopack_export_namespace__, c: __turbopack_cache__, M: __turbopack_modules__, l: __turbopack_load__, j: __turbopack_dynamic__, P: __turbopack_resolve_absolute_path__, U: __turbopack_relative_url__, R: __turbopack_resolve_module_id_path__, g: global, __dirname, x: __turbopack_external_require__, y: __turbopack_external_import__ }) => (() => {
"use strict";

__turbopack_esm__({
    "AuthProvider": ()=>AuthProvider,
    "useAuth": ()=>useAuth
});
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$server$2f$future$2f$route$2d$modules$2f$app$2d$page$2f$vendored$2f$rsc$2f$react$2d$server$2d$dom$2d$turbopack$2d$server$2d$edge$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__ = __turbopack_import__("[project]/node_modules/next/dist/server/future/route-modules/app-page/vendored/rsc/react-server-dom-turbopack-server-edge.js [app-rsc] (ecmascript)");
"__TURBOPACK__ecmascript__hoisting__location__";
;
const AuthProvider = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$server$2f$future$2f$route$2d$modules$2f$app$2d$page$2f$vendored$2f$rsc$2f$react$2d$server$2d$dom$2d$turbopack$2d$server$2d$edge$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["registerClientReference"])(function() {
    throw new Error("Attempted to call AuthProvider() from the server but AuthProvider is on the client. It's not possible to invoke a client function from the server, it can only be rendered as a Component or passed to props of a Client Component.");
}, "[project]/src/context/auth-context.tsx", "AuthProvider");
const useAuth = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$server$2f$future$2f$route$2d$modules$2f$app$2d$page$2f$vendored$2f$rsc$2f$react$2d$server$2d$dom$2d$turbopack$2d$server$2d$edge$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["registerClientReference"])(function() {
    throw new Error("Attempted to call useAuth() from the server but useAuth is on the client. It's not possible to invoke a client function from the server, it can only be rendered as a Component or passed to props of a Client Component.");
}, "[project]/src/context/auth-context.tsx", "useAuth");

})()),
"[project]/src/context/auth-context.tsx [app-rsc] (ecmascript)": (({ r: __turbopack_require__, f: __turbopack_module_context__, i: __turbopack_import__, s: __turbopack_esm__, v: __turbopack_export_value__, n: __turbopack_export_namespace__, c: __turbopack_cache__, M: __turbopack_modules__, l: __turbopack_load__, j: __turbopack_dynamic__, P: __turbopack_resolve_absolute_path__, U: __turbopack_relative_url__, R: __turbopack_resolve_module_id_path__, g: global, __dirname, x: __turbopack_external_require__, y: __turbopack_external_import__ }) => (() => {
"use strict";

var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$context$2f$auth$2d$context$2e$tsx__$28$client__proxy$29$__ = __turbopack_import__("[project]/src/context/auth-context.tsx (client proxy)");
"__TURBOPACK__ecmascript__hoisting__location__";
"TURBOPACK { transition: next-ecmascript-client-reference }";
;
__turbopack_export_namespace__(__TURBOPACK__imported__module__$5b$project$5d2f$src$2f$context$2f$auth$2d$context$2e$tsx__$28$client__proxy$29$__);

})()),
"[project]/src/context/language-context.tsx (client proxy)": (({ r: __turbopack_require__, f: __turbopack_module_context__, i: __turbopack_import__, s: __turbopack_esm__, v: __turbopack_export_value__, n: __turbopack_export_namespace__, c: __turbopack_cache__, M: __turbopack_modules__, l: __turbopack_load__, j: __turbopack_dynamic__, P: __turbopack_resolve_absolute_path__, U: __turbopack_relative_url__, R: __turbopack_resolve_module_id_path__, g: global, __dirname, x: __turbopack_external_require__, y: __turbopack_external_import__ }) => (() => {
"use strict";

__turbopack_esm__({
    "LanguageProvider": ()=>LanguageProvider,
    "useLanguage": ()=>useLanguage
});
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$server$2f$future$2f$route$2d$modules$2f$app$2d$page$2f$vendored$2f$rsc$2f$react$2d$server$2d$dom$2d$turbopack$2d$server$2d$edge$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__ = __turbopack_import__("[project]/node_modules/next/dist/server/future/route-modules/app-page/vendored/rsc/react-server-dom-turbopack-server-edge.js [app-rsc] (ecmascript)");
"__TURBOPACK__ecmascript__hoisting__location__";
;
const LanguageProvider = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$server$2f$future$2f$route$2d$modules$2f$app$2d$page$2f$vendored$2f$rsc$2f$react$2d$server$2d$dom$2d$turbopack$2d$server$2d$edge$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["registerClientReference"])(function() {
    throw new Error("Attempted to call LanguageProvider() from the server but LanguageProvider is on the client. It's not possible to invoke a client function from the server, it can only be rendered as a Component or passed to props of a Client Component.");
}, "[project]/src/context/language-context.tsx", "LanguageProvider");
const useLanguage = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$server$2f$future$2f$route$2d$modules$2f$app$2d$page$2f$vendored$2f$rsc$2f$react$2d$server$2d$dom$2d$turbopack$2d$server$2d$edge$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["registerClientReference"])(function() {
    throw new Error("Attempted to call useLanguage() from the server but useLanguage is on the client. It's not possible to invoke a client function from the server, it can only be rendered as a Component or passed to props of a Client Component.");
}, "[project]/src/context/language-context.tsx", "useLanguage");

})()),
"[project]/src/context/language-context.tsx [app-rsc] (ecmascript)": (({ r: __turbopack_require__, f: __turbopack_module_context__, i: __turbopack_import__, s: __turbopack_esm__, v: __turbopack_export_value__, n: __turbopack_export_namespace__, c: __turbopack_cache__, M: __turbopack_modules__, l: __turbopack_load__, j: __turbopack_dynamic__, P: __turbopack_resolve_absolute_path__, U: __turbopack_relative_url__, R: __turbopack_resolve_module_id_path__, g: global, __dirname, x: __turbopack_external_require__, y: __turbopack_external_import__ }) => (() => {
"use strict";

var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$context$2f$language$2d$context$2e$tsx__$28$client__proxy$29$__ = __turbopack_import__("[project]/src/context/language-context.tsx (client proxy)");
"__TURBOPACK__ecmascript__hoisting__location__";
"TURBOPACK { transition: next-ecmascript-client-reference }";
;
__turbopack_export_namespace__(__TURBOPACK__imported__module__$5b$project$5d2f$src$2f$context$2f$language$2d$context$2e$tsx__$28$client__proxy$29$__);

})()),
"[project]/src/lib/server/platform-data.ts [app-rsc] (ecmascript)": (({ r: __turbopack_require__, f: __turbopack_module_context__, i: __turbopack_import__, s: __turbopack_esm__, v: __turbopack_export_value__, n: __turbopack_export_namespace__, c: __turbopack_cache__, M: __turbopack_modules__, l: __turbopack_load__, j: __turbopack_dynamic__, P: __turbopack_resolve_absolute_path__, U: __turbopack_relative_url__, R: __turbopack_resolve_module_id_path__, g: global, __dirname, x: __turbopack_external_require__, y: __turbopack_external_import__ }) => (() => {
"use strict";

__turbopack_esm__({
    "buildAdminSnapshot": ()=>buildAdminSnapshot,
    "buildAnalyticsSnapshot": ()=>buildAnalyticsSnapshot,
    "buildDashboardSnapshot": ()=>buildDashboardSnapshot,
    "buildDispatchQueue": ()=>buildDispatchQueue,
    "buildWeatherOutlook": ()=>buildWeatherOutlook,
    "ensurePlatformBaseline": ()=>ensurePlatformBaseline,
    "getBaselineBuyerInteractions": ()=>getBaselineBuyerInteractions,
    "getBaselineBuyers": ()=>getBaselineBuyers,
    "getBaselineFinanceTransactions": ()=>getBaselineFinanceTransactions,
    "getBaselineListings": ()=>getBaselineListings,
    "getBaselineLoans": ()=>getBaselineLoans,
    "getBaselinePosts": ()=>getBaselinePosts,
    "getBaselinePredictions": ()=>getBaselinePredictions,
    "getBaselinePrices": ()=>getBaselinePrices,
    "getBaselineProducts": ()=>getBaselineProducts
});
const BASE_TIME = new Date('2026-03-15T05:30:00.000Z').getTime();
const commodityCatalog = [
    {
        name: 'Maize',
        basePrice: 2240,
        seasonalBias: 1.02,
        demand: [
            'Feed',
            'Starch',
            'Poultry'
        ],
        tags: [
            'grain',
            'feed'
        ]
    },
    {
        name: 'Wheat',
        basePrice: 2475,
        seasonalBias: 0.99,
        demand: [
            'Flour mills',
            'Bulk trade'
        ],
        tags: [
            'staple',
            'rabi'
        ]
    },
    {
        name: 'Soybean',
        basePrice: 4620,
        seasonalBias: 1.03,
        demand: [
            'Oil extraction',
            'Meal'
        ],
        tags: [
            'oilseed',
            'protein'
        ]
    },
    {
        name: 'Paddy',
        basePrice: 2135,
        seasonalBias: 1.01,
        demand: [
            'Rice mills',
            'Parboiling'
        ],
        tags: [
            'kharif',
            'milling'
        ]
    },
    {
        name: 'Cotton',
        basePrice: 7140,
        seasonalBias: 0.98,
        demand: [
            'Ginners',
            'Spinners'
        ],
        tags: [
            'fibre',
            'textile'
        ]
    },
    {
        name: 'Mustard',
        basePrice: 5980,
        seasonalBias: 1.04,
        demand: [
            'Oil mills',
            'Seed trade'
        ],
        tags: [
            'oilseed',
            'rabi'
        ]
    },
    {
        name: 'Tomato',
        basePrice: 1680,
        seasonalBias: 1.07,
        demand: [
            'Fresh retail',
            'Processors'
        ],
        tags: [
            'vegetable',
            'perishable'
        ]
    },
    {
        name: 'Onion',
        basePrice: 1960,
        seasonalBias: 1.02,
        demand: [
            'Wholesale',
            'Storage'
        ],
        tags: [
            'vegetable',
            'storage'
        ]
    },
    {
        name: 'Potato',
        basePrice: 1540,
        seasonalBias: 1.01,
        demand: [
            'Fresh retail',
            'Processing'
        ],
        tags: [
            'vegetable',
            'processing'
        ]
    }
];
const mandiMarkets = [
    'Pune APMC',
    'Nashik APMC',
    'Indore APMC',
    'Jaipur APMC',
    'Lucknow Mandi',
    'Hisar Grain Market',
    'Nagpur APMC',
    'Bhopal Krishi Upaj Mandi',
    'Kanpur Grain Hub'
];
const districts = [
    'Pune',
    'Nashik',
    'Indore',
    'Jaipur',
    'Lucknow',
    'Nagpur',
    'Hisar',
    'Bhopal',
    'Kanpur'
];
const forumCategories = [
    'Pricing',
    'Pest Watch',
    'Irrigation',
    'Buyer Leads',
    'Storage',
    'Soil Health'
];
function stableIso(offsetHours) {
    return new Date(BASE_TIME + offsetHours * 60 * 60 * 1000).toISOString();
}
function stableId(prefix, key) {
    return `${prefix}_${key.toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, '')}`;
}
function dedupeBy(items, key) {
    const seen = new Set();
    return items.filter((item)=>{
        const signature = key(item);
        if (seen.has(signature)) return false;
        seen.add(signature);
        return true;
    });
}
function mergeBy(current, baseline, key) {
    const existing = new Set(current.map(key));
    return [
        ...current,
        ...baseline.filter((item)=>!existing.has(key(item)))
    ];
}
function getBaselineProducts() {
    const items = [
        {
            id: stableId('prd', 'hybrid-maize-seed-k900'),
            name: 'Hybrid Maize Seed K-900',
            category: 'Seeds',
            description: 'High-vigour hybrid for rainfed and irrigated belts with strong standability.',
            imageHint: 'maize seed bag',
            seller: 'Bharat Seed Network',
            location: 'Indore',
            sku: 'SEED-MAIZE-K900',
            price: 2450,
            currency: 'INR',
            unit: 'pack',
            rating: 4.8,
            inStock: true,
            stockQuantity: 180,
            moq: 1,
            leadTimeDays: 2,
            tags: [
                'hybrid',
                'kharif',
                'high-germination'
            ],
            createdAt: stableIso(0),
            updatedAt: stableIso(48)
        },
        {
            id: stableId('prd', 'balanced-npk-19-19-19'),
            name: 'Balanced NPK 19-19-19',
            category: 'Fertilizers',
            description: 'Water-soluble balanced fertilizer for vegetative growth and recovery cycles.',
            imageHint: 'fertilizer bag farm',
            seller: 'Agro Input Depot',
            location: 'Pune',
            sku: 'FERT-NPK-191919',
            price: 1980,
            currency: 'INR',
            unit: '25kg',
            rating: 4.6,
            inStock: true,
            stockQuantity: 260,
            moq: 1,
            leadTimeDays: 1,
            tags: [
                'water-soluble',
                'macro-nutrients'
            ],
            createdAt: stableIso(1),
            updatedAt: stableIso(49)
        },
        {
            id: stableId('prd', 'drip-irrigation-kit'),
            name: 'Drip Irrigation Starter Kit',
            category: 'Irrigation',
            description: 'Starter pack for one-acre vegetable and orchard plots with filter set included.',
            imageHint: 'drip irrigation field',
            seller: 'MicroFlow Agri Systems',
            location: 'Jaipur',
            sku: 'IRR-DRIP-ACRE1',
            price: 18950,
            currency: 'INR',
            unit: 'set',
            rating: 4.7,
            inStock: true,
            stockQuantity: 34,
            moq: 1,
            leadTimeDays: 4,
            tags: [
                'water-saving',
                'orchard',
                'vegetables'
            ],
            createdAt: stableIso(2),
            updatedAt: stableIso(50)
        },
        {
            id: stableId('prd', 'soil-test-kit-pro'),
            name: 'Soil Test Kit Pro',
            category: 'Testing',
            description: 'Portable field kit for pH, EC, nitrogen, phosphorus, and potassium screening.',
            imageHint: 'soil testing kit',
            seller: 'Krishi Lab Supplies',
            location: 'Nagpur',
            sku: 'TEST-SOIL-PRO',
            price: 3290,
            currency: 'INR',
            unit: 'kit',
            rating: 4.5,
            inStock: true,
            stockQuantity: 92,
            moq: 1,
            leadTimeDays: 2,
            tags: [
                'soil-health',
                'portable'
            ],
            createdAt: stableIso(3),
            updatedAt: stableIso(51)
        },
        {
            id: stableId('prd', 'neem-biopesticide'),
            name: 'Neem Biopesticide Concentrate',
            category: 'Crop Protection',
            description: 'Botanical pest management concentrate for sucking pests and early infestation control.',
            imageHint: 'biopesticide bottle agriculture',
            seller: 'GreenShield Crop Care',
            location: 'Lucknow',
            sku: 'BIO-NEEM-1L',
            price: 845,
            currency: 'INR',
            unit: 'litre',
            rating: 4.4,
            inStock: true,
            stockQuantity: 140,
            moq: 2,
            leadTimeDays: 2,
            tags: [
                'bio-input',
                'integrated-pest-management'
            ],
            createdAt: stableIso(4),
            updatedAt: stableIso(52)
        },
        {
            id: stableId('prd', 'mulch-film-25-micron'),
            name: 'Silver Black Mulch Film 25 Micron',
            category: 'Crop Protection',
            description: 'Weed suppression and moisture conservation film for high-value crops.',
            imageHint: 'mulch film farm',
            seller: 'FieldCover Solutions',
            location: 'Kanpur',
            sku: 'MULCH-25M-400',
            price: 3820,
            currency: 'INR',
            unit: 'roll',
            rating: 4.3,
            inStock: true,
            stockQuantity: 76,
            moq: 1,
            leadTimeDays: 3,
            tags: [
                'moisture-retention',
                'weed-control'
            ],
            createdAt: stableIso(5),
            updatedAt: stableIso(53)
        }
    ];
    commodityCatalog.forEach((commodity, index)=>{
        items.push({
            id: stableId('prd', `${commodity.name}-premium-lot-crates`),
            name: `${commodity.name} Handling Crates`,
            category: 'Post Harvest',
            description: `Reusable stackable crates optimized for ${commodity.name.toLowerCase()} handling and dispatch.`,
            imageHint: `${commodity.name.toLowerCase()} crates warehouse`,
            seller: 'Harvest Logistics Supplies',
            location: districts[index % districts.length],
            sku: `POST-${commodity.name.toUpperCase().replace(/[^A-Z0-9]/g, '')}-CRT`,
            price: 540 + index * 45,
            currency: 'INR',
            unit: 'set of 10',
            rating: 4.1 + index % 5 * 0.1,
            inStock: true,
            stockQuantity: 50 + index * 8,
            moq: 1,
            leadTimeDays: 2 + index % 3,
            tags: [
                commodity.tags[0],
                'storage',
                'dispatch'
            ],
            createdAt: stableIso(10 + index),
            updatedAt: stableIso(58 + index)
        });
    });
    return dedupeBy(items, (item)=>item.id);
}
function getBaselineListings() {
    return commodityCatalog.flatMap((commodity, commodityIndex)=>districts.slice(0, 3).map((district, districtIndex)=>{
            const quantity = 18 + commodityIndex * 4 + districtIndex * 3;
            const basePrice = Math.round(commodity.basePrice * commodity.seasonalBias + districtIndex * 25);
            const idKey = `${commodity.name}-${district}-${districtIndex}`;
            return {
                id: stableId('lst', idKey),
                name: `${commodity.name} ${districtIndex === 0 ? 'FAQ' : districtIndex === 1 ? 'Premium' : 'Fresh Arrival'} Lot`,
                commodity: commodity.name,
                farmerName: [
                    'Ravi Patel',
                    'Suman Yadav',
                    'Harjit Singh',
                    'Kiran Pawar',
                    'Ayesha Khan'
                ][commodityIndex % 5],
                description: `${quantity} MT available from ${district}. ${commodity.demand[0]} grade with dispatch readiness in 24-48 hours.`,
                imageHint: `${commodity.name.toLowerCase()} farm harvest`,
                price: basePrice,
                unit: 'INR/quintal',
                location: district,
                quantity,
                grade: districtIndex === 0 ? 'FAQ' : districtIndex === 1 ? 'Grade A' : 'Premium',
                harvestDate: stableIso(-(commodityIndex * 12 + districtIndex * 8)),
                pricingMode: districtIndex === 2 ? 'market-linked' : districtIndex === 1 ? 'negotiable' : 'fixed',
                priceFloor: basePrice - 90,
                priceCeiling: basePrice + 140,
                moisture: 10 + (commodityIndex + districtIndex) % 5,
                buyerInterestCount: 4 + commodityIndex + districtIndex,
                status: commodityIndex % 4 === 0 && districtIndex === 2 ? 'pending' : 'active',
                createdAt: stableIso(-(commodityIndex * 10 + districtIndex * 4)),
                updatedAt: stableIso(commodityIndex * 6 + districtIndex)
            };
        }));
}
function getBaselineBuyers() {
    const buyers = [
        [
            'Pune Grain Processors Pvt Ltd',
            'Pune, Maharashtra',
            [
                'Maize',
                'Soybean',
                'Chana'
            ],
            '1,200 MT / month',
            'Processor'
        ],
        [
            'Nashik Fresh Chains',
            'Nashik, Maharashtra',
            [
                'Tomato',
                'Onion',
                'Potato'
            ],
            '420 MT / week',
            'Retail Aggregator'
        ],
        [
            'Indore Feed Ingredients',
            'Indore, Madhya Pradesh',
            [
                'Maize',
                'Wheat'
            ],
            '900 MT / month',
            'Feed Mill'
        ],
        [
            'Jaipur Cotton Link',
            'Jaipur, Rajasthan',
            [
                'Cotton',
                'Mustard'
            ],
            '680 MT / month',
            'Exporter'
        ],
        [
            'Lucknow Agro Retail Hub',
            'Lucknow, Uttar Pradesh',
            [
                'Paddy',
                'Potato'
            ],
            '500 MT / month',
            'Wholesaler'
        ],
        [
            'Central India Oil Mills',
            'Bhopal, Madhya Pradesh',
            [
                'Soybean',
                'Mustard'
            ],
            '1,050 MT / month',
            'Oil Mill'
        ],
        [
            'Kanpur Staples Network',
            'Kanpur, Uttar Pradesh',
            [
                'Wheat',
                'Paddy'
            ],
            '1,480 MT / month',
            'Bulk Buyer'
        ],
        [
            'Nagpur Fresh Basket',
            'Nagpur, Maharashtra',
            [
                'Tomato',
                'Onion'
            ],
            '270 MT / week',
            'Retail Aggregator'
        ],
        [
            'Hisar Fibre Traders',
            'Hisar, Haryana',
            [
                'Cotton',
                'Mustard'
            ],
            '760 MT / month',
            'Trader'
        ]
    ];
    return buyers.map((buyer, index)=>({
            id: stableId('buy', buyer[0]),
            name: buyer[0],
            location: buyer[1],
            demand: [
                ...buyer[2]
            ],
            preferredLocations: districts.filter((_, districtIndex)=>districtIndex % 2 === index % 2).slice(0, 4),
            specialties: buyer[2].map((item)=>`${item} bulk sourcing`),
            capacity: buyer[3],
            type: buyer[4],
            verified: index !== 8,
            contact: `procurement${index + 1}@${buyer[0].toLowerCase().replace(/[^a-z0-9]+/g, '')}.in`,
            phone: `+91 98${(index + 21).toString().padStart(2, '0')}45${(8100 + index * 37).toString().padStart(4, '0')}`,
            paymentTerms: index % 3 === 0 ? 'T+2 bank transfer' : index % 3 === 1 ? '50% advance, balance on weighment' : 'UPI / RTGS within 24h',
            rating: 4.1 + index % 5 * 0.15,
            trades: 18 + index * 9,
            status: index === 8 ? 'Pending' : 'Verified',
            lastActiveAt: stableIso(-index * 6),
            createdAt: stableIso(-(72 + index * 4)),
            updatedAt: stableIso(index * 3)
        }));
}
function getBaselinePrices() {
    return commodityCatalog.flatMap((commodity, commodityIndex)=>mandiMarkets.map((market, marketIndex)=>{
            const multiplier = 1 + (marketIndex % 4 - 1.5) * 0.012 + commodityIndex % 3 * 0.01;
            const previousPrice = Math.round(commodity.basePrice * multiplier);
            const currentPrice = Math.round(previousPrice * (1 + ((marketIndex + commodityIndex) % 5 - 2) * 0.012));
            const changePct = Number(((currentPrice - previousPrice) / previousPrice * 100).toFixed(2));
            return {
                id: stableId('prc', `${commodity.name}-${market}`),
                commodity: commodity.name,
                market,
                price: currentPrice,
                previousPrice,
                arrivalsTons: 110 + commodityIndex * 28 + marketIndex * 17,
                unit: 'INR/quintal',
                changePct,
                trend: changePct > 0.15 ? 'up' : changePct < -0.15 ? 'down' : 'stable',
                timestamp: stableIso(-(marketIndex * 4 + commodityIndex))
            };
        }));
}
function getBaselinePosts() {
    const templates = [
        'Need moisture benchmark before dispatch to mill buyer',
        'Which spray window worked after late rainfall?',
        'Storage strategy for 3-week holding without quality loss',
        'Buyer asked for tighter grading. How are you sorting?',
        'What mandi spread are you seeing against local village trade?',
        'Need quick peer advice on irrigation gap during heat stress'
    ];
    return Array.from({
        length: 18
    }, (_, index)=>{
        const category = forumCategories[index % forumCategories.length];
        const district = districts[index % districts.length];
        const commodity = commodityCatalog[index % commodityCatalog.length].name;
        return {
            id: stableId('pst', `${category}-${district}-${index}`),
            authorName: [
                'Regular Farmer',
                'Savita Growers Group',
                'Field Officer Manoj',
                'Kisan Producer Co.'
            ][index % 4],
            authorEmail: index % 4 === 0 ? 'farmer@farmersmarketplace.app' : undefined,
            category,
            region: district,
            title: `${commodity}: ${templates[index % templates.length]}`,
            content: `Sharing field update from ${district}. Current lot is ${commodity.toLowerCase()} with active buyer interest, and I want benchmark responses on pricing, logistics, and crop handling before the next dispatch window.`,
            likes: 8 + index * 3,
            comments: 2 + index % 6,
            createdAt: stableIso(-(index * 5)),
            updatedAt: stableIso(-(index * 5) + 2)
        };
    });
}
function getBaselineBuyerInteractions() {
    const buyers = getBaselineBuyers();
    return buyers.slice(0, 10).map((buyer, index)=>({
            id: stableId('int', `${buyer.name}-${index}`),
            buyerId: buyer.id,
            buyerName: buyer.name,
            senderEmail: index % 2 === 0 ? 'farmer@farmersmarketplace.app' : 'buyer.user@farmersmarketplace.app',
            senderName: index % 2 === 0 ? 'Regular Farmer' : 'Agro Processor',
            mode: index % 3 === 0 ? 'call' : 'message',
            lotDetails: `${commodityCatalog[index % commodityCatalog.length].name} ${(20 + index * 2).toString()} MT, ${index % 2 === 0 ? 'Grade A' : 'FAQ'}`,
            message: index % 3 === 0 ? 'Need a 15-minute call to confirm dispatch slot, moisture, and weighbridge window.' : 'Interested in current rate, payment terms, and expected unloading schedule for this lot.',
            status: index % 3 === 0 ? 'scheduled' : index % 3 === 1 ? 'pending' : 'completed',
            createdAt: stableIso(-(index * 7))
        }));
}
function getBaselineFinanceTransactions() {
    const flows = [
        {
            id: stableId('txn', 'farmer-wallet-credit'),
            userEmail: 'farmer@farmersmarketplace.app',
            type: 'Initial wallet credit',
            amount: 50000,
            direction: 'in',
            method: 'system',
            status: 'completed',
            createdAt: stableIso(-120)
        },
        {
            id: stableId('txn', 'farmer-marketplace-payout'),
            userEmail: 'farmer@farmersmarketplace.app',
            type: 'Marketplace settlement payout',
            amount: 126500,
            direction: 'in',
            method: 'RTGS',
            status: 'completed',
            createdAt: stableIso(-78)
        },
        {
            id: stableId('txn', 'farmer-input-purchase'),
            userEmail: 'farmer@farmersmarketplace.app',
            type: 'Input purchase',
            amount: 18950,
            direction: 'out',
            method: 'UPI',
            status: 'completed',
            createdAt: stableIso(-60)
        },
        {
            id: stableId('txn', 'buyer-escrow-funding'),
            userEmail: 'buyer.user@farmersmarketplace.app',
            type: 'Buyer wallet funding',
            amount: 220000,
            direction: 'in',
            method: 'netbanking',
            status: 'completed',
            createdAt: stableIso(-90)
        },
        {
            id: stableId('txn', 'buyer-lot-booking'),
            userEmail: 'buyer.user@farmersmarketplace.app',
            type: 'Advance lot booking',
            amount: 68000,
            direction: 'out',
            method: 'card',
            status: 'completed',
            createdAt: stableIso(-32)
        }
    ];
    return flows;
}
function getBaselineLoans() {
    return [
        {
            id: 'LOAN-842310',
            userEmail: 'farmer@farmersmarketplace.app',
            userName: 'Regular Farmer',
            amount: 280000,
            purpose: 'Drip irrigation and fertigation retrofit',
            risk: 'Medium',
            status: 'Approved',
            createdAt: stableIso(-220),
            updatedAt: stableIso(-180)
        },
        {
            id: 'LOAN-842355',
            userEmail: 'farmer@farmersmarketplace.app',
            userName: 'Regular Farmer',
            amount: 125000,
            purpose: 'Working capital for post-harvest storage',
            risk: 'Low',
            status: 'Pending',
            createdAt: stableIso(-72),
            updatedAt: stableIso(-12)
        },
        {
            id: 'LOAN-842390',
            userEmail: 'buyer.user@farmersmarketplace.app',
            userName: 'Agro Processor',
            amount: 650000,
            purpose: 'Procurement line for maize and soybean aggregation',
            risk: 'High',
            status: 'Pending',
            createdAt: stableIso(-48),
            updatedAt: stableIso(-8)
        }
    ];
}
function getBaselinePredictions() {
    return commodityCatalog.flatMap((commodity, index)=>[
            {
                id: stableId('pred', `${commodity.name}-price`),
                type: 'price',
                userEmail: 'farmer@farmersmarketplace.app',
                inputSummary: `${commodity.name} in ${mandiMarkets[index % mandiMarkets.length]}`,
                outputSummary: `${index % 2 === 0 ? 'High' : 'Medium'} confidence, ${index % 3 === 0 ? 'Hold for 5-7 days' : 'Stagger sales and monitor daily'}`,
                createdAt: stableIso(-(index * 9))
            },
            {
                id: stableId('pred', `${commodity.name}-disease`),
                type: 'disease',
                userEmail: 'farmer@farmersmarketplace.app',
                inputSummary: `${commodity.name} field scan`,
                outputSummary: index % 2 === 0 ? 'Plant appears largely healthy with minor stress symptoms' : 'Early leaf blight risk detected',
                createdAt: stableIso(-(index * 9 + 2))
            },
            {
                id: stableId('pred', `${commodity.name}-soil`),
                type: 'soil',
                userEmail: 'farmer@farmersmarketplace.app',
                inputSummary: `${commodity.name} soil sample`,
                outputSummary: `${index % 4 === 0 ? 'Excellent' : index % 4 === 1 ? 'Good' : 'Moderate'} soil (${74 + index}/100)`,
                createdAt: stableIso(-(index * 9 + 4))
            }
        ]);
}
function ensurePlatformBaseline(db) {
    db.products = mergeBy(db.products, getBaselineProducts(), (item)=>item.id);
    db.listings = mergeBy(db.listings, getBaselineListings(), (item)=>item.id);
    db.buyers = mergeBy(db.buyers, getBaselineBuyers(), (item)=>item.id);
    db.prices = mergeBy(db.prices, getBaselinePrices(), (item)=>item.id);
    db.posts = mergeBy(db.posts, getBaselinePosts(), (item)=>item.id);
    db.predictions = mergeBy(db.predictions, getBaselinePredictions(), (item)=>item.id);
    db.buyerInteractions = mergeBy(db.buyerInteractions, getBaselineBuyerInteractions(), (item)=>item.id);
    db.financeTransactions = mergeBy(db.financeTransactions, getBaselineFinanceTransactions(), (item)=>item.id);
    db.loanApplications = mergeBy(db.loanApplications, getBaselineLoans(), (item)=>item.id);
    db.config = {
        accessPolicy: {
            farmerPlan: db.config.accessPolicy.farmerPlan || 'Verified farmer workspace with AI, market, and finance access',
            buyerPlan: db.config.accessPolicy.buyerPlan || 'Verified buyer workspace with sourcing, negotiation, and dispatch tools'
        },
        categories: {
            crops: Array.from(new Set([
                ...db.config.categories.crops,
                ...commodityCatalog.map((item)=>item.name)
            ])).sort(),
            inputs: Array.from(new Set([
                ...db.config.categories.inputs,
                'Seeds',
                'Fertilizers',
                'Irrigation',
                'Crop Protection',
                'Testing',
                'Post Harvest'
            ]))
        },
        ui: {
            supportedLanguages: db.config.ui?.supportedLanguages?.length ? db.config.ui.supportedLanguages : [
                'en',
                'hi'
            ],
            defaultLanguage: db.config.ui?.defaultLanguage || 'en'
        },
        finance: {
            settlementAccountName: db.config.finance?.settlementAccountName || 'Farmer Marketplace Escrow Desk',
            settlementAccountNumber: db.config.finance?.settlementAccountNumber || '221100987654321',
            settlementIfsc: db.config.finance?.settlementIfsc || 'SBIN0001543',
            settlementUpi: db.config.finance?.settlementUpi || 'farmersmarketplace@upi'
        },
        developer: {
            name: db.config.developer?.name || 'Nishchal Mishra',
            photoUrl: db.config.developer?.photoUrl || '',
            college: db.config.developer?.college || 'Project Developer',
            rollNo: db.config.developer?.rollNo || '',
            address: db.config.developer?.address || 'India',
            contact: db.config.developer?.contact || '',
            bio: db.config.developer?.bio || 'Builder of the Farmer Marketplace platform and AgriTrust workflow.'
        }
    };
    return db;
}
function buildWeatherOutlook() {
    return districts.map((district, index)=>{
        const rainfallMm = 2 + index * 7 % 19;
        const high = 29 + index % 5;
        const low = 18 + index % 4;
        const humidity = 52 + index * 3;
        return {
            district,
            high,
            low,
            humidity,
            rainfallMm,
            condition: rainfallMm >= 15 ? 'Rain watch' : rainfallMm >= 8 ? 'Cloud build-up' : 'Mostly clear',
            advisory: rainfallMm >= 15 ? 'Delay foliar spray and plan harvest cover.' : rainfallMm >= 8 ? 'Prefer morning loading and monitor fungal pressure.' : 'Good window for harvest, drying, and dispatch.'
        };
    });
}
function buildDispatchQueue(input) {
    const buyers = input.buyers.filter((buyer)=>buyer.verified).slice(0, 6);
    return input.listings.slice(0, 8).map((listing, index)=>({
            id: `DSP-${(410 + index).toString().padStart(3, '0')}`,
            lot: listing.name,
            commodity: listing.commodity || listing.name,
            source: listing.location,
            destination: buyers[index % buyers.length]?.location.split(',')[0] || 'Pune',
            buyerName: buyers[index % buyers.length]?.name || 'Verified Buyer',
            quantity: listing.quantity ?? 20,
            etaHours: 6 + index * 2,
            status: index % 3 === 0 ? 'Ready' : index % 3 === 1 ? 'In Transit' : 'Scheduled'
        }));
}
function buildFieldSignalSummary(reports) {
    return reports.slice().sort((a, b)=>b.updatedAt.localeCompare(a.updatedAt)).slice(0, 6).map((report)=>({
            id: report.id,
            city: report.city,
            reportType: report.reportType,
            commodity: report.commodity ?? 'Local crop mix',
            summary: report.summary,
            rewardAmount: report.rewardAmount,
            status: report.status,
            reportedPrice: report.reportedPrice,
            weatherCondition: report.weatherCondition
        }));
}
function buildShipmentMoments(shipments) {
    return shipments.slice().sort((a, b)=>b.updatedAt.localeCompare(a.updatedAt)).slice(0, 6).map((shipment)=>({
            id: shipment.id,
            orderId: shipment.orderId,
            status: shipment.status,
            routeCode: shipment.routeCode ?? 'Route pending',
            destination: shipment.destination,
            checkpoint: shipment.lastCheckpoint,
            etaHours: shipment.etaHours,
            deliveryVerified: shipment.deliveryVerified,
            latestUpdate: shipment.checkpoints?.[0]?.updatedAt ?? shipment.updatedAt
        }));
}
function buildDashboardSnapshot(input) {
    const activeListings = input.listings.filter((listing)=>listing.status === 'active');
    const activeBuyers = input.buyers.filter((buyer)=>buyer.status !== 'Suspended');
    const completedTransactions = input.financeTransactions.filter((txn)=>txn.status === 'completed');
    const topPrices = [
        ...input.prices
    ].sort((a, b)=>b.price - a.price).slice(0, 3);
    const fieldSignals = buildFieldSignalSummary(input.fieldReports);
    const shipmentMoments = buildShipmentMoments(input.shipments);
    const trustReady = input.profiles.filter((profile)=>profile.trustScore >= 90).length;
    const pendingTrust = input.agriTrustSubmissions.filter((submission)=>submission.status === 'submitted').length;
    return {
        missionCards: [
            {
                title: 'Market Prices',
                href: '/prices',
                meta: `${input.prices.length} mandi rates`
            },
            {
                title: 'Marketplace',
                href: '/marketplace',
                meta: `${activeListings.length} live lots`
            },
            {
                title: 'AI Agronomist',
                href: '/agronomist',
                meta: `${input.predictions.length} logged analyses`
            },
            {
                title: 'Buyers',
                href: '/buyers',
                meta: `${activeBuyers.length} active buyers`
            }
        ],
        alerts: [
            `${shipmentMoments.filter((item)=>item.status === 'in_transit').length} shipments are currently in transit`,
            `${fieldSignals.filter((item)=>item.reportType === 'weather').length} farmer weather signals arrived for review`,
            `${input.buyerInteractions.filter((item)=>item.status === 'pending').length} buyer conversations need follow-up`
        ],
        aiUpdates: [
            `Top mandi rate today: ${topPrices[0]?.commodity ?? 'Maize'} at INR ${topPrices[0]?.price.toLocaleString() ?? '0'}/quintal`,
            `${input.predictions.length} AI inference logs available for review and audit`,
            `${activeListings.filter((listing)=>(listing.buyerInterestCount ?? 0) >= 8).length} lots show strong buyer demand`
        ],
        rolePulse: {
            trustReady,
            unreadNotifications: input.notifications.filter((item)=>!item.read).length,
            shipmentBacklog: input.shipments.filter((item)=>item.status !== 'delivered').length
        },
        trustQueue: {
            pendingForms: pendingTrust,
            verifiedProfiles: trustReady,
            newFieldReports: input.fieldReports.filter((report)=>report.status === 'submitted').length
        },
        fieldSignals,
        shipmentMoments,
        coverageDistricts: new Set([
            ...input.listings.map((listing)=>listing.location),
            ...districts
        ]).size,
        tradePipelineValue: activeListings.reduce((sum, listing)=>sum + listing.price * (listing.quantity ?? 1), 0),
        aiInferences: input.predictions.length,
        completedSettlements: completedTransactions.length
    };
}
function buildAnalyticsSnapshot(input) {
    const weather = buildWeatherOutlook();
    const currentWeather = weather[0];
    const fieldSignals = buildFieldSignalSummary(input.fieldReports);
    const forecast = weather.slice(0, 5).map((item, index)=>({
            day: [
                'Mon',
                'Tue',
                'Wed',
                'Thu',
                'Fri',
                'Sat',
                'Sun'
            ][index],
            temp: item.high,
            condition: item.condition,
            wind: `${9 + index} km/h`
        }));
    const commoditySeries = commodityCatalog.slice(0, 5).map((commodity, index)=>{
        const listingMatches = input.listings.filter((listing)=>(listing.commodity || listing.name).toLowerCase().includes(commodity.name.toLowerCase()));
        const avgPrice = input.prices.filter((price)=>price.commodity === commodity.name).reduce((sum, row, _, all)=>sum + row.price / Math.max(all.length, 1), 0);
        return {
            season: `2026-Q${index + 1}`,
            [commodity.name.toLowerCase()]: Number((avgPrice / 850 + listingMatches.length * 0.22).toFixed(1))
        };
    });
    const spendingData = [
        'Jan',
        'Feb',
        'Mar',
        'Apr',
        'May',
        'Jun'
    ].map((month, index)=>({
            month,
            seeds: 80000 + input.listings.length * 3200 + index * 22000,
            fertilizers: 110000 + input.prices.length * 1800 + index * 26000,
            agrochemicals: 42000 + input.predictions.length * 1200 + index * 9000
        }));
    const totalOrderValue = input.orders.reduce((sum, order)=>sum + order.totalAmount, 0);
    const paidOrderValue = input.payments.filter((payment)=>payment.status === 'paid').reduce((sum, payment)=>sum + payment.amount, 0);
    const revenueForecastData = [
        'Aug',
        'Sep',
        'Oct',
        'Nov',
        'Dec'
    ].map((month, index)=>({
            month,
            revenue: Math.round((paidOrderValue || totalOrderValue || 1500000) * (0.7 + index * 0.12))
        }));
    const reports = [
        {
            id: 'profit-loss',
            name: 'Trade and Settlement Summary',
            description: `${input.orders.length} orders, ${input.payments.length} payments, and INR ${paidOrderValue.toLocaleString()} settled.`
        },
        {
            id: 'input-spend',
            name: 'Input Spend and Working Capital',
            description: `${input.financeTransactions.length} ledger entries across wallet funding, purchase outflow, and settlements.`
        }
    ];
    const roleMix = [
        {
            role: 'Farmers',
            count: input.profiles.filter((profile)=>profile.role === 'user').length
        },
        {
            role: 'Buyers',
            count: input.profiles.filter((profile)=>profile.role === 'buyer').length
        },
        {
            role: 'Admins',
            count: input.profiles.filter((profile)=>profile.role === 'admin').length
        }
    ];
    const shipmentStatus = [
        'processing',
        'packed',
        'in_transit',
        'out_for_delivery',
        'delivered'
    ].map((status)=>({
            status,
            count: input.shipments.filter((shipment)=>shipment.status === status).length
        }));
    return {
        weather: {
            city: currentWeather?.district || 'Pune',
            current: {
                temp: currentWeather?.high ?? 30,
                condition: currentWeather?.condition ?? 'Mostly clear',
                wind: '12 km/h'
            },
            forecast
        },
        yieldData: commoditySeries,
        spendingData,
        revenueForecastData,
        reports,
        roleMix,
        shipmentStatus,
        fieldSignals,
        trustMetrics: {
            verifiedProfiles: input.profiles.filter((profile)=>profile.trustScore >= 90).length,
            pendingProfiles: input.profiles.filter((profile)=>profile.trustScore < 90).length + input.agriTrustSubmissions.filter((submission)=>submission.status === 'submitted').length,
            unreadNotifications: input.notifications.filter((notification)=>!notification.read).length
        }
    };
}
function buildAdminSnapshot(input) {
    const dailyActiveParticipants = input.users.length + input.buyers.length;
    const grossTradeVolume = input.orders.reduce((sum, order)=>sum + order.totalAmount, 0);
    const creditDisbursedToday = input.financeTransactions.filter((transaction)=>transaction.direction === 'in').reduce((sum, transaction)=>sum + transaction.amount, 0);
    const escalationAlerts = input.buyerInteractions.filter((interaction)=>interaction.status === 'pending').length;
    const dispatchQueue = buildDispatchQueue({
        listings: input.listings,
        buyers: input.buyers
    });
    const trustQueue = input.agriTrustSubmissions.filter((submission)=>submission.status === 'submitted').slice(0, 6).map((submission)=>({
            id: submission.id,
            userName: submission.userName,
            role: submission.role,
            submittedAt: submission.createdAt,
            proofNote: submission.proofNote || 'Submitted via profile vault'
        }));
    const fieldSignals = buildFieldSignalSummary(input.fieldReports);
    const shipmentMoments = buildShipmentMoments(input.shipments);
    return {
        kpis: [
            {
                title: 'Daily Active Participants',
                value: dailyActiveParticipants.toLocaleString(),
                change: `${input.users.length} users + ${input.buyers.length} buyers`
            },
            {
                title: 'Gross Trade Volume',
                value: `INR ${(grossTradeVolume / 10000000).toFixed(2)} Cr`,
                change: `${input.orders.length} tracked orders`
            },
            {
                title: 'Credit Disbursed',
                value: `INR ${(creditDisbursedToday / 100000).toFixed(1)} L`,
                change: `${input.financeTransactions.length} ledger inflows`
            },
            {
                title: 'Escalation Alerts',
                value: escalationAlerts.toString(),
                change: `${input.notifications.filter((item)=>!item.read).length} unread notifications · ${input.agriTrustSubmissions.filter((item)=>item.status === 'submitted').length} trust reviews`
            }
        ],
        dispatchQueue,
        trustQueue,
        fieldSignals,
        shipmentMoments,
        liveOps: {
            shipmentsOpen: input.shipments.filter((shipment)=>shipment.status !== 'delivered').length,
            trustVerified: input.profiles.filter((profile)=>profile.trustScore >= 90).length,
            fundingQueue: input.walletFundingRequests.filter((request)=>request.status !== 'verified' && request.status !== 'rejected').length
        }
    };
}

})()),
"[project]/src/lib/server/pricing.ts [app-rsc] (ecmascript)": (({ r: __turbopack_require__, f: __turbopack_module_context__, i: __turbopack_import__, s: __turbopack_esm__, v: __turbopack_export_value__, n: __turbopack_export_namespace__, c: __turbopack_cache__, M: __turbopack_modules__, l: __turbopack_load__, j: __turbopack_dynamic__, P: __turbopack_resolve_absolute_path__, U: __turbopack_relative_url__, R: __turbopack_resolve_module_id_path__, g: global, __dirname, x: __turbopack_external_require__, y: __turbopack_external_import__ }) => (() => {
"use strict";

__turbopack_esm__({
    "enrichCommodityPrice": ()=>enrichCommodityPrice,
    "explainPriceDiscovery": ()=>explainPriceDiscovery,
    "getCommodityMsp": ()=>getCommodityMsp
});
const MSP_BY_COMMODITY = {
    maize: 2225,
    wheat: 2425,
    soybean: 4892,
    paddy: 2300,
    cotton: 7121,
    mustard: 5950,
    tomato: 1400,
    onion: 1800,
    potato: 1500
};
function clamp(value, min, max) {
    return Math.max(min, Math.min(max, value));
}
function getCommodityMsp(commodity) {
    return MSP_BY_COMMODITY[commodity.trim().toLowerCase()] ?? 2200;
}
function enrichCommodityPrice(row, history) {
    const commodity = row.commodity.trim().toLowerCase();
    const related = history.filter((item)=>item.commodity.trim().toLowerCase() === commodity);
    const local = related.filter((item)=>item.market.toLowerCase() === row.market.toLowerCase());
    const relevant = local.length ? local : related;
    const avgArrivals = relevant.reduce((sum, item)=>sum + (item.arrivalsTons ?? 0), 0) / Math.max(relevant.length, 1);
    const avgChange = relevant.reduce((sum, item)=>sum + item.changePct, 0) / Math.max(relevant.length, 1);
    const avgPrice = relevant.reduce((sum, item)=>sum + item.price, 0) / Math.max(relevant.length, 1);
    const msp = row.baseMsp ?? getCommodityMsp(row.commodity);
    const localDemandIndex = clamp(0.5 + avgChange / 12 + (avgPrice > msp ? 0.12 : -0.04), 0, 1);
    const localSupplySaturationIndex = clamp(((row.arrivalsTons ?? avgArrivals) || 0) / Math.max(avgArrivals * 1.8, 1), 0, 1);
    const alpha = clamp(0.24 + Math.max(avgChange, 0) / 20, 0.18, 0.48);
    const beta = clamp(0.18 + Math.abs(Math.min(avgChange, 0)) / 20 + localSupplySaturationIndex * 0.08, 0.14, 0.42);
    const heuristicSuggestedPrice = Math.round(msp * (1 + alpha * localDemandIndex - beta * localSupplySaturationIndex));
    return {
        ...row,
        baseMsp: msp,
        localDemandIndex: Number(localDemandIndex.toFixed(2)),
        localSupplySaturationIndex: Number(localSupplySaturationIndex.toFixed(2)),
        alpha: Number(alpha.toFixed(2)),
        beta: Number(beta.toFixed(2)),
        heuristicSuggestedPrice,
        source: row.source ?? 'heuristic'
    };
}
function explainPriceDiscovery(row) {
    const msp = row.baseMsp ?? getCommodityMsp(row.commodity);
    const alpha = row.alpha ?? 0.24;
    const beta = row.beta ?? 0.18;
    const demand = row.localDemandIndex ?? 0.5;
    const supply = row.localSupplySaturationIndex ?? 0.5;
    const suggested = row.heuristicSuggestedPrice ?? Math.round(msp * (1 + alpha * demand - beta * supply));
    return {
        formula: 'Psuggested = Pbase × (1 + alpha × Dlocal - beta × Slocal)',
        breakdown: [
            `Pbase (MSP): INR ${msp.toLocaleString()}`,
            `Dlocal: ${demand.toFixed(2)}`,
            `Slocal: ${supply.toFixed(2)}`,
            `alpha: ${alpha.toFixed(2)}`,
            `beta: ${beta.toFixed(2)}`
        ],
        suggested
    };
}

})()),
"[project]/src/lib/server/store.ts [app-rsc] (ecmascript)": (({ r: __turbopack_require__, f: __turbopack_module_context__, i: __turbopack_import__, s: __turbopack_esm__, v: __turbopack_export_value__, n: __turbopack_export_namespace__, c: __turbopack_cache__, M: __turbopack_modules__, l: __turbopack_load__, j: __turbopack_dynamic__, P: __turbopack_resolve_absolute_path__, U: __turbopack_relative_url__, R: __turbopack_resolve_module_id_path__, g: global, __dirname, x: __turbopack_external_require__, y: __turbopack_external_import__ }) => (() => {
"use strict";

__turbopack_esm__({
    "platformStore": ()=>platformStore
});
var __TURBOPACK__commonjs__external__node$3a$fs__ = __turbopack_external_require__("node:fs", true);
var __TURBOPACK__commonjs__external__node$3a$path__ = __turbopack_external_require__("node:path", true);
var __TURBOPACK__commonjs__external__node$3a$crypto__ = __turbopack_external_require__("node:crypto", true);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$netlify$2f$blobs$2f$dist$2f$main$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__$3c$module__evaluation$3e$__ = __turbopack_import__("[project]/node_modules/@netlify/blobs/dist/main.js [app-rsc] (ecmascript) <module evaluation>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$netlify$2f$blobs$2f$dist$2f$main$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_import__("[project]/node_modules/@netlify/blobs/dist/main.js [app-rsc] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$server$2f$platform$2d$data$2e$ts__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__ = __turbopack_import__("[project]/src/lib/server/platform-data.ts [app-rsc] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$server$2f$pricing$2e$ts__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__ = __turbopack_import__("[project]/src/lib/server/pricing.ts [app-rsc] (ecmascript)");
"__TURBOPACK__ecmascript__hoisting__location__";
;
;
;
;
;
;
const DATA_DIR = __TURBOPACK__commonjs__external__node$3a$path__["default"].join(process.cwd(), 'data');
const DB_FILE = __TURBOPACK__commonjs__external__node$3a$path__["default"].join(DATA_DIR, 'platform-db.json');
const SESSION_COOKIE_NAME = 'fm_session';
const SESSION_TTL_SECONDS = 60 * 60 * 24 * 7;
const APP_SECRET = process.env.APP_SECRET || 'dev-insecure-secret-change-me';
const OTP_TTL_MINUTES = 10;
const OTP_ATTEMPT_LIMIT = 5;
const OTP_RESEND_COOLDOWN_SECONDS = 45;
const OTP_SEND_LIMIT = 5;
const ADMIN_SUPERKEY = process.env.AGRI_TRUST_ADMIN_SUPERKEY || 'AGRI-TRUST-ADMIN';
const NETLIFY_DB_STORE_NAME = process.env.NETLIFY_DB_STORE_NAME || 'farmers-marketplace-data';
const NETLIFY_DB_KEY = 'platform-db';
const NETLIFY_DB_WRITE_RETRIES = 6;
let mutationQueue = Promise.resolve();
let dbCache = null;
let dbReadPromise = null;
let flushTimer = null;
let flushChain = Promise.resolve();
let dbDirty = false;
const nowIso = ()=>new Date().toISOString();
const id = (prefix)=>`${prefix}_${__TURBOPACK__commonjs__external__node$3a$crypto__["default"].randomBytes(8).toString('hex')}`;
function normalizeEmail(email) {
    return email.trim().toLowerCase();
}
function normalizeName(name) {
    return name.trim().replace(/\s+/g, ' ');
}
function generateOtp() {
    return __TURBOPACK__commonjs__external__node$3a$crypto__["default"].randomInt(0, 1_000_000).toString().padStart(6, '0');
}
function hashPassword(password) {
    const salt = __TURBOPACK__commonjs__external__node$3a$crypto__["default"].randomBytes(16).toString('hex');
    const hash = __TURBOPACK__commonjs__external__node$3a$crypto__["default"].scryptSync(password, salt, 64).toString('hex');
    return `${salt}:${hash}`;
}
function verifyPassword(password, passwordHash) {
    const [salt, stored] = passwordHash.split(':');
    if (!salt || !stored) return false;
    const actual = __TURBOPACK__commonjs__external__node$3a$crypto__["default"].scryptSync(password, salt, 64).toString('hex');
    return __TURBOPACK__commonjs__external__node$3a$crypto__["default"].timingSafeEqual(Buffer.from(actual, 'hex'), Buffer.from(stored, 'hex'));
}
function hashOtp(challengeId, otp) {
    return __TURBOPACK__commonjs__external__node$3a$crypto__["default"].createHmac('sha256', APP_SECRET).update(`${challengeId}:${otp}`).digest('hex');
}
function verifyOtp(challengeId, otp, otpHash) {
    const actual = hashOtp(challengeId, otp);
    return __TURBOPACK__commonjs__external__node$3a$crypto__["default"].timingSafeEqual(Buffer.from(actual, 'hex'), Buffer.from(otpHash, 'hex'));
}
function maskEmail(email) {
    const [localPart, domain = ''] = email.split('@');
    if (!localPart) return email;
    const visibleLocal = localPart.length <= 2 ? `${localPart[0] ?? '*'}*` : `${localPart.slice(0, 2)}${'*'.repeat(Math.max(1, localPart.length - 2))}`;
    const [domainName, ...rest] = domain.split('.');
    const visibleDomain = domainName ? `${domainName.slice(0, 1)}${'*'.repeat(Math.max(1, domainName.length - 1))}` : '';
    return `${visibleLocal}@${[
        visibleDomain,
        ...rest
    ].filter(Boolean).join('.')}`;
}
function isExpired(timestamp) {
    return new Date(timestamp).getTime() <= Date.now();
}
function secondsUntil(timestamp) {
    return Math.max(0, Math.ceil((new Date(timestamp).getTime() - Date.now()) / 1000));
}
function signPayload(payload) {
    return __TURBOPACK__commonjs__external__node$3a$crypto__["default"].createHmac('sha256', APP_SECRET).update(payload).digest('hex');
}
function b64urlEncode(value) {
    return Buffer.from(value).toString('base64url');
}
function b64urlDecode(value) {
    return Buffer.from(value, 'base64url').toString();
}
function createSessionToken(user) {
    const exp = Math.floor(Date.now() / 1000) + SESSION_TTL_SECONDS;
    const payloadObj = {
        sub: user.id,
        email: user.email,
        role: user.role,
        exp
    };
    const payload = b64urlEncode(JSON.stringify(payloadObj));
    const sig = signPayload(payload);
    return `${payload}.${sig}`;
}
function verifySessionToken(token) {
    const [payload, sig] = token.split('.');
    if (!payload || !sig) return null;
    const expected = signPayload(payload);
    if (!__TURBOPACK__commonjs__external__node$3a$crypto__["default"].timingSafeEqual(Buffer.from(sig), Buffer.from(expected))) return null;
    try {
        const parsed = JSON.parse(b64urlDecode(payload));
        if (!parsed.exp || parsed.exp < Math.floor(Date.now() / 1000)) return null;
        return {
            email: parsed.email,
            role: parsed.role
        };
    } catch  {
        return null;
    }
}
function toSafeUser(user) {
    const { passwordHash, ...safe } = user;
    return safe;
}
function createAppUser(name, email, role, passwordHash) {
    const timestamp = nowIso();
    return {
        id: id('usr'),
        name: normalizeName(name),
        email: normalizeEmail(email),
        passwordHash,
        role,
        status: 'active',
        isPro: true,
        aiCredits: 9999,
        createdAt: timestamp,
        updatedAt: timestamp
    };
}
function createBuyerProfileForUser(user) {
    const timestamp = nowIso();
    return {
        id: id('buy'),
        name: user.name,
        location: 'Profile pending completion',
        demand: [],
        capacity: 'Not provided',
        type: 'Buyer',
        verified: false,
        contact: user.email,
        trades: 0,
        status: 'Pending',
        createdAt: timestamp,
        updatedAt: timestamp
    };
}
function defaultNotificationPreferences() {
    return {
        priceAlerts: true,
        marketplace: true,
        community: true,
        operations: true
    };
}
function roleDisplay(role) {
    return role === 'user' ? 'Farmer' : role === 'buyer' ? 'Buyer' : 'Admin';
}
function buildTrustScore(profile) {
    if (profile.role === 'user') {
        if (profile.verification?.farmer?.status === 'verified') return 94;
        if (profile.farmerProfile?.aadhaarNumber) return 78;
    }
    if (profile.role === 'buyer') {
        if (profile.verification?.buyer?.status === 'verified') return 92;
        if (profile.buyerProfile?.gstin) return 76;
    }
    if (profile.role === 'admin') return 98;
    return 48;
}
function buildTrustLabel(score) {
    return score >= 95 ? 'Enterprise verified' : score >= 90 ? 'Verified' : score >= 70 ? 'Review pending' : 'Profile incomplete';
}
function createWorkspaceProfileForUser(user) {
    const timestamp = nowIso();
    const base = {
        id: id('prf'),
        userEmail: normalizeEmail(user.email),
        role: user.role,
        displayName: user.name,
        phone: '',
        city: user.role === 'buyer' ? 'Pune' : user.role === 'admin' ? 'New Delhi' : 'Nashik',
        state: user.role === 'admin' ? 'Delhi' : 'Maharashtra',
        address: '',
        about: user.role === 'buyer' ? 'Procurement workspace pending GST verification.' : user.role === 'admin' ? 'Platform operations workspace.' : 'Farm profile pending Agri Trust onboarding.',
        preferredLanguage: 'en',
        notificationPreferences: defaultNotificationPreferences(),
        trustScore: 48,
        trustLabel: 'Profile incomplete',
        verification: {
            farmer: null,
            buyer: null,
            admin: null
        },
        createdAt: timestamp,
        updatedAt: timestamp
    };
    if (user.role === 'user') {
        base.farmerProfile = {
            aadhaarNumber: '',
            farmSizeAcres: 0,
            primaryCrops: [
                'Maize'
            ],
            mandiPreference: 'Pune APMC'
        };
    }
    if (user.role === 'buyer') {
        base.buyerProfile = {
            gstin: '',
            businessName: user.name,
            procurementCapacity: '0 MT/month',
            preferredCommodities: [
                'Maize'
            ]
        };
    }
    if (user.role === 'admin') {
        base.adminProfile = {
            superKey: '',
            department: 'Operations',
            accessPasscodeHint: 'Use the issued Agri Trust admin key.'
        };
    }
    return base;
}
function ensureWorkspaceProfile(db, user) {
    const email = normalizeEmail(user.email);
    const existingIndex = db.profiles.findIndex((profile)=>normalizeEmail(profile.userEmail) === email);
    if (existingIndex < 0) {
        db.profiles.unshift(normalizeWorkspaceProfileForRole(createWorkspaceProfileForUser(user)));
        return;
    }
    db.profiles[existingIndex] = normalizeWorkspaceProfileForRole({
        ...db.profiles[existingIndex],
        displayName: db.profiles[existingIndex].displayName || user.name,
        role: user.role,
        updatedAt: nowIso()
    });
}
function createNotification(input) {
    return {
        id: id('ntf'),
        read: false,
        createdAt: nowIso(),
        ...input
    };
}
function listAdminEmails(db) {
    return Array.from(new Set(db.users.filter((user)=>user.role === 'admin').map((user)=>normalizeEmail(user.email)).filter(Boolean)));
}
function notifyAdmins(db, input) {
    for (const adminEmail of listAdminEmails(db)){
        db.notifications.unshift(createNotification({
            userEmail: adminEmail,
            ...input
        }));
    }
}
function normalizeWorkspaceProfileForRole(profile) {
    const next = {
        ...profile,
        verification: {
            farmer: profile.verification.farmer,
            buyer: profile.verification.buyer,
            admin: profile.verification.admin
        }
    };
    if (next.role === 'admin') {
        next.farmerProfile = undefined;
        next.buyerProfile = undefined;
        next.verification = {
            farmer: null,
            buyer: null,
            admin: null
        };
        next.adminProfile = {
            superKey: '',
            department: next.adminProfile?.department || 'Operations',
            accessPasscodeHint: next.adminProfile?.accessPasscodeHint || 'Administrator controls verification and platform settings.'
        };
    }
    if (next.role === 'user') {
        next.buyerProfile = undefined;
        next.adminProfile = undefined;
        next.verification = {
            farmer: next.verification.farmer,
            buyer: null,
            admin: null
        };
    }
    if (next.role === 'buyer') {
        next.farmerProfile = undefined;
        next.adminProfile = undefined;
        next.verification = {
            farmer: null,
            buyer: next.verification.buyer,
            admin: null
        };
    }
    const trustScore = buildTrustScore(next);
    next.trustScore = trustScore;
    next.trustLabel = buildTrustLabel(trustScore);
    return next;
}
function createShipmentCheckpoint(status, note, location, updatedBy) {
    return {
        status,
        note,
        location,
        updatedBy,
        updatedAt: nowIso()
    };
}
function ensureBuyerProfile(db, user) {
    const email = normalizeEmail(user.email);
    const alreadyExists = db.buyers.some((buyer)=>normalizeEmail(buyer.contact ?? '') === email);
    if (!alreadyExists) {
        db.buyers.unshift(createBuyerProfileForUser(user));
    }
}
function pruneOtpChallenges(db) {
    db.otpChallenges = db.otpChallenges.filter((challenge)=>!isExpired(challenge.expiresAt) && challenge.attempts < OTP_ATTEMPT_LIMIT);
}
function createOtpDispatch(challenge) {
    return {
        requestToken: challenge.id,
        purpose: challenge.purpose,
        role: challenge.role,
        destination: maskEmail(challenge.email),
        expiresAt: challenge.expiresAt,
        resendAvailableAt: challenge.resendAvailableAt,
        sendCount: challenge.sendCount,
        maxSendCount: OTP_SEND_LIMIT
    };
}
function seedDb() {
    const timestamp = nowIso();
    const users = [
        {
            id: id('usr'),
            name: 'Admin User',
            email: 'admin@farmersmarketplace.app',
            passwordHash: hashPassword('adminpass'),
            role: 'admin',
            status: 'active',
            isPro: true,
            aiCredits: 9999,
            createdAt: timestamp,
            updatedAt: timestamp
        },
        {
            id: id('usr'),
            name: 'Regular Farmer',
            email: 'farmer@farmersmarketplace.app',
            passwordHash: hashPassword('farmerpass'),
            role: 'user',
            status: 'active',
            isPro: true,
            aiCredits: 9999,
            createdAt: timestamp,
            updatedAt: timestamp
        },
        {
            id: id('usr'),
            name: 'Agro Processor',
            email: 'buyer.user@farmersmarketplace.app',
            passwordHash: hashPassword('buyerpass'),
            role: 'buyer',
            status: 'active',
            isPro: true,
            aiCredits: 9999,
            createdAt: timestamp,
            updatedAt: timestamp
        },
        {
            id: id('usr'),
            name: 'Google User',
            email: 'google.user@farmersmarketplace.app',
            passwordHash: hashPassword('googlepass'),
            role: 'user',
            status: 'active',
            isPro: true,
            aiCredits: 9999,
            createdAt: timestamp,
            updatedAt: timestamp
        }
    ];
    const profiles = users.map((user)=>createWorkspaceProfileForUser(toSafeUser(user)));
    const adminProfile = profiles.find((profile)=>profile.role === 'admin');
    const farmerProfile = profiles.find((profile)=>profile.userEmail === 'farmer@farmersmarketplace.app');
    const buyerWorkspace = profiles.find((profile)=>profile.role === 'buyer');
    if (adminProfile?.adminProfile) {
        adminProfile.adminProfile.superKey = ADMIN_SUPERKEY;
        adminProfile.trustScore = buildTrustScore(adminProfile);
        adminProfile.trustLabel = buildTrustLabel(adminProfile.trustScore);
        adminProfile.verification.admin = {
            status: 'verified',
            label: 'Agri Trust superkey accepted',
            submittedAt: timestamp,
            verifiedAt: timestamp
        };
    }
    if (farmerProfile?.farmerProfile) {
        farmerProfile.farmerProfile.aadhaarNumber = '512345678901';
        farmerProfile.farmerProfile.farmSizeAcres = 14;
        farmerProfile.farmerProfile.primaryCrops = [
            'Maize',
            'Soybean',
            'Onion'
        ];
        farmerProfile.trustScore = buildTrustScore(farmerProfile);
        farmerProfile.trustLabel = buildTrustLabel(farmerProfile.trustScore);
        farmerProfile.verification.farmer = {
            status: 'verified',
            label: 'Aadhaar submitted and validated',
            submittedAt: timestamp,
            verifiedAt: timestamp
        };
    }
    if (buyerWorkspace?.buyerProfile) {
        buyerWorkspace.buyerProfile.gstin = '27ABCDE1234F1Z5';
        buyerWorkspace.buyerProfile.procurementCapacity = '1200 MT/month';
        buyerWorkspace.buyerProfile.preferredCommodities = [
            'Maize',
            'Soybean',
            'Wheat'
        ];
        buyerWorkspace.trustScore = buildTrustScore(buyerWorkspace);
        buyerWorkspace.trustLabel = buildTrustLabel(buyerWorkspace.trustScore);
        buyerWorkspace.verification.buyer = {
            status: 'verified',
            label: 'GSTIN submitted and validated',
            submittedAt: timestamp,
            verifiedAt: timestamp
        };
    }
    return {
        users,
        profiles,
        otpChallenges: [],
        products: [
            {
                id: id('prd'),
                name: 'Hybrid Maize Seed K-900',
                category: 'Seeds',
                price: 2450,
                currency: 'INR',
                unit: 'pack',
                rating: 4.8,
                inStock: true,
                createdAt: timestamp,
                updatedAt: timestamp
            },
            {
                id: id('prd'),
                name: 'Balanced NPK 19-19-19',
                category: 'Fertilizers',
                price: 1980,
                currency: 'INR',
                unit: '25kg',
                rating: 4.6,
                inStock: true,
                createdAt: timestamp,
                updatedAt: timestamp
            }
        ],
        listings: [
            {
                id: id('lst'),
                name: 'Fresh Maize (100kg)',
                farmerName: 'Ravi Patel',
                price: 2350,
                unit: 'quintal',
                location: 'Pune',
                status: 'active',
                createdAt: timestamp,
                updatedAt: timestamp
            }
        ],
        buyers: [
            {
                id: id('buy'),
                name: 'Pune Grain Processors Pvt Ltd',
                location: 'Pune, Maharashtra',
                demand: [
                    'Maize',
                    'Soybean',
                    'Chana'
                ],
                capacity: '1,200 MT / month',
                type: 'Processor',
                verified: true,
                contact: 'procurement@punegrainprocessors.in',
                trades: 48,
                status: 'Verified',
                createdAt: timestamp,
                updatedAt: timestamp
            }
        ],
        prices: [
            {
                id: id('prc'),
                commodity: 'Maize',
                market: 'Pune APMC',
                price: 2150,
                unit: 'INR/quintal',
                changePct: 2.5,
                trend: 'up',
                timestamp
            },
            {
                id: id('prc'),
                commodity: 'Wheat',
                market: 'Indore APMC',
                price: 2480,
                unit: 'INR/quintal',
                changePct: -1.2,
                trend: 'down',
                timestamp
            }
        ],
        posts: [
            {
                id: id('pst'),
                authorName: 'Regular Farmer',
                authorEmail: 'farmer@farmersmarketplace.app',
                title: 'Best irrigation schedule for late sowing?',
                content: 'Looking for practical irrigation schedules for maize in delayed monsoon conditions.',
                likes: 14,
                comments: 5,
                createdAt: timestamp,
                updatedAt: timestamp
            }
        ],
        orders: [],
        payments: [],
        predictions: [],
        buyerInteractions: [],
        priceAlerts: [],
        notifications: [
            createNotification({
                userEmail: 'farmer@farmersmarketplace.app',
                title: 'Agri Trust approved',
                body: 'Your farmer workspace is verified and ready for buyer-facing trade.',
                category: 'system',
                actionHref: '/profile'
            }),
            createNotification({
                userEmail: 'buyer.user@farmersmarketplace.app',
                title: 'New verified lots available',
                body: 'Fresh maize and soybean lots are now available in the marketplace.',
                category: 'trade',
                actionHref: '/marketplace'
            }),
            createNotification({
                userEmail: 'admin@farmersmarketplace.app',
                title: 'Command center synced',
                body: 'Admin command center is using the live platform data store.',
                category: 'ops',
                actionHref: '/admin/dashboard'
            })
        ],
        shipments: [],
        invoices: [],
        financeTransactions: [
            {
                id: id('txn'),
                userEmail: 'farmer@farmersmarketplace.app',
                type: 'Initial wallet credit',
                amount: 50000,
                direction: 'in',
                method: 'system',
                status: 'completed',
                createdAt: timestamp
            }
        ],
        walletFundingRequests: [],
        loanApplications: [],
        negotiations: [],
        agriTrustForms: [
            {
                id: id('atf'),
                title: 'Farmer identity refresh',
                description: 'Confirm Aadhaar, mandi preference, and dispatch contact details before the next buyer cycle.',
                audienceRole: 'user',
                fields: [
                    {
                        id: id('fld'),
                        label: 'Aadhaar number',
                        type: 'text',
                        required: true,
                        placeholder: '12-digit Aadhaar number'
                    },
                    {
                        id: id('fld'),
                        label: 'Preferred mandi',
                        type: 'text',
                        required: true,
                        placeholder: 'Example: Pune APMC'
                    },
                    {
                        id: id('fld'),
                        label: 'Dispatch contact phone',
                        type: 'text',
                        required: true,
                        placeholder: 'Primary phone for logistics confirmation'
                    }
                ],
                createdBy: 'admin@farmersmarketplace.app',
                status: 'open',
                createdAt: timestamp,
                updatedAt: timestamp
            }
        ],
        agriTrustSubmissions: [],
        fieldReports: [],
        communicationGroups: [],
        communicationGroupMessages: [],
        config: {
            accessPolicy: {
                farmerPlan: 'Free for all users',
                buyerPlan: 'Free for all users'
            },
            categories: {
                crops: [
                    'Maize',
                    'Beans',
                    'Coffee',
                    'Cassava',
                    'Banana'
                ],
                inputs: [
                    'Seeds',
                    'Fertilizers',
                    'Agrochemicals',
                    'Tools'
                ]
            },
            ui: {
                supportedLanguages: [
                    'en',
                    'hi'
                ],
                defaultLanguage: 'en'
            },
            finance: {
                settlementAccountName: 'Farmer Marketplace Escrow Desk',
                settlementAccountNumber: '221100987654321',
                settlementIfsc: 'SBIN0001543',
                settlementUpi: 'farmersmarketplace@upi'
            },
            developer: {
                name: '',
                photoUrl: '',
                college: '',
                rollNo: '',
                address: '',
                contact: '',
                bio: ''
            }
        }
    };
}
async function ensureDbFile() {
    await __TURBOPACK__commonjs__external__node$3a$fs__["promises"].mkdir(DATA_DIR, {
        recursive: true
    });
    try {
        await __TURBOPACK__commonjs__external__node$3a$fs__["promises"].access(DB_FILE);
    } catch  {
        await __TURBOPACK__commonjs__external__node$3a$fs__["promises"].writeFile(DB_FILE, JSON.stringify(seedDb(), null, 2), 'utf8');
    }
}
function normalizeDb(parsed) {
    const timestamp = nowIso();
    return (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$server$2f$platform$2d$data$2e$ts__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["ensurePlatformBaseline"])({
        users: (parsed.users ?? []).map((user)=>({
                ...user,
                status: user.status ?? 'active',
                isPro: user.isPro ?? true,
                aiCredits: user.aiCredits ?? 9999,
                createdAt: user.createdAt ?? timestamp,
                updatedAt: user.updatedAt ?? user.createdAt ?? timestamp
            })),
        profiles: (parsed.profiles ?? []).map((profile)=>{
            const trustScore = profile.trustScore ?? buildTrustScore(profile);
            return {
                ...profile,
                preferredLanguage: profile.preferredLanguage ?? 'en',
                notificationPreferences: profile.notificationPreferences ?? defaultNotificationPreferences(),
                trustScore,
                trustLabel: profile.trustLabel ?? buildTrustLabel(trustScore),
                verification: profile.verification ?? {
                    farmer: null,
                    buyer: null,
                    admin: null
                },
                createdAt: profile.createdAt ?? timestamp,
                updatedAt: profile.updatedAt ?? profile.createdAt ?? timestamp
            };
        }),
        otpChallenges: (parsed.otpChallenges ?? []).map((challenge)=>({
                ...challenge,
                resendAvailableAt: challenge.resendAvailableAt ?? challenge.updatedAt ?? challenge.createdAt ?? timestamp,
                lastSentAt: challenge.lastSentAt ?? challenge.updatedAt ?? challenge.createdAt ?? timestamp,
                sendCount: challenge.sendCount ?? 1
            })),
        products: parsed.products ?? [],
        listings: parsed.listings ?? [],
        buyers: parsed.buyers ?? [],
        prices: parsed.prices ?? [],
        posts: parsed.posts ?? [],
        orders: parsed.orders ?? [],
        payments: parsed.payments ?? [],
        predictions: parsed.predictions ?? [],
        buyerInteractions: parsed.buyerInteractions ?? [],
        priceAlerts: parsed.priceAlerts ?? [],
        notifications: parsed.notifications ?? [],
        shipments: (parsed.shipments ?? []).map((shipment)=>({
                ...shipment,
                routeCode: shipment.routeCode ?? `RT-${shipment.id?.slice(-5) ?? '00000'}`,
                vehicleId: shipment.vehicleId ?? 'AGFLEET-01',
                checkpoints: shipment.checkpoints?.length ? shipment.checkpoints : [
                    createShipmentCheckpoint(shipment.status ?? 'processing', shipment.lastCheckpoint ?? 'Shipment created', shipment.origin ?? 'Dispatch node')
                ],
                deliveryVerified: shipment.deliveryVerified ?? Boolean(shipment.acknowledgedAt),
                updatedAt: shipment.updatedAt ?? timestamp,
                createdAt: shipment.createdAt ?? timestamp
            })),
        invoices: parsed.invoices ?? [],
        financeTransactions: parsed.financeTransactions ?? [],
        walletFundingRequests: parsed.walletFundingRequests ?? [],
        loanApplications: parsed.loanApplications ?? [],
        negotiations: parsed.negotiations ?? [],
        agriTrustForms: parsed.agriTrustForms ?? [],
        agriTrustSubmissions: parsed.agriTrustSubmissions ?? [],
        fieldReports: parsed.fieldReports ?? [],
        communicationGroups: parsed.communicationGroups ?? [],
        communicationGroupMessages: parsed.communicationGroupMessages ?? [],
        config: {
            accessPolicy: {
                farmerPlan: parsed.config?.accessPolicy?.farmerPlan ?? 'Free for all users',
                buyerPlan: parsed.config?.accessPolicy?.buyerPlan ?? 'Free for all users'
            },
            categories: {
                crops: parsed.config?.categories?.crops ?? [],
                inputs: parsed.config?.categories?.inputs ?? []
            },
            ui: {
                supportedLanguages: parsed.config?.ui?.supportedLanguages ?? [
                    'en',
                    'hi'
                ],
                defaultLanguage: parsed.config?.ui?.defaultLanguage ?? 'en'
            },
            finance: {
                settlementAccountName: parsed.config?.finance?.settlementAccountName ?? 'Farmer Marketplace Escrow Desk',
                settlementAccountNumber: parsed.config?.finance?.settlementAccountNumber ?? '221100987654321',
                settlementIfsc: parsed.config?.finance?.settlementIfsc ?? 'SBIN0001543',
                settlementUpi: parsed.config?.finance?.settlementUpi ?? 'farmersmarketplace@upi'
            },
            developer: {
                name: parsed.config?.developer?.name ?? '',
                photoUrl: parsed.config?.developer?.photoUrl ?? '',
                college: parsed.config?.developer?.college ?? '',
                rollNo: parsed.config?.developer?.rollNo ?? '',
                address: parsed.config?.developer?.address ?? '',
                contact: parsed.config?.developer?.contact ?? '',
                bio: parsed.config?.developer?.bio ?? ''
            }
        }
    });
}
function cloneDb(db) {
    return JSON.parse(JSON.stringify(db));
}
function shouldUseNetlifyBlobs() {
    return process.env.NETLIFY === 'true' || Boolean(process.env.BLOB_READ_WRITE_TOKEN);
}
function getNetlifyDbStore() {
    return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$netlify$2f$blobs$2f$dist$2f$main$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__$3c$locals$3e$__["getStore"])({
        name: NETLIFY_DB_STORE_NAME,
        consistency: 'strong'
    });
}
async function loadInitialDb() {
    try {
        const raw = await __TURBOPACK__commonjs__external__node$3a$fs__["promises"].readFile(DB_FILE, 'utf8');
        return normalizeDb(JSON.parse(raw));
    } catch  {
        return seedDb();
    }
}
async function readDbFromNetlify() {
    const store = getNetlifyDbStore();
    const existing = await store.get(NETLIFY_DB_KEY, {
        type: 'json',
        consistency: 'strong'
    });
    if (!existing) {
        const initial = await loadInitialDb();
        await store.setJSON(NETLIFY_DB_KEY, initial, {
            onlyIfNew: true
        });
        const seeded = await store.get(NETLIFY_DB_KEY, {
            type: 'json',
            consistency: 'strong'
        });
        const parsed = normalizeDb(seeded ?? initial);
        pruneOtpChallenges(parsed);
        return parsed;
    }
    const parsed = normalizeDb(existing);
    pruneOtpChallenges(parsed);
    return parsed;
}
async function updateDbInNetlify(mutator) {
    const store = getNetlifyDbStore();
    for(let attempt = 0; attempt < NETLIFY_DB_WRITE_RETRIES; attempt += 1){
        const existing = await store.getWithMetadata(NETLIFY_DB_KEY, {
            type: 'json',
            consistency: 'strong'
        });
        const current = normalizeDb(existing?.data ?? await loadInitialDb());
        pruneOtpChallenges(current);
        const next = cloneDb(current);
        mutator(next);
        const result = existing ? await store.setJSON(NETLIFY_DB_KEY, next, {
            onlyIfMatch: existing.etag
        }) : await store.setJSON(NETLIFY_DB_KEY, next, {
            onlyIfNew: true
        });
        if (result.modified) {
            return next;
        }
    }
    throw new Error('Database update failed after repeated Netlify Blob write conflicts.');
}
async function loadDbFromDisk() {
    await ensureDbFile();
    const raw = await __TURBOPACK__commonjs__external__node$3a$fs__["promises"].readFile(DB_FILE, 'utf8');
    const parsed = normalizeDb(JSON.parse(raw));
    pruneOtpChallenges(parsed);
    dbCache = parsed;
    return parsed;
}
async function readDb() {
    if (shouldUseNetlifyBlobs()) {
        return readDbFromNetlify();
    }
    if (dbCache) return dbCache;
    if (!dbReadPromise) {
        dbReadPromise = loadDbFromDisk().finally(()=>{
            dbReadPromise = null;
        });
    }
    return dbReadPromise;
}
async function flushDbToDisk() {
    if (!dbCache || !dbDirty) return;
    dbDirty = false;
    await ensureDbFile();
    const tmpFile = `${DB_FILE}.tmp`;
    await __TURBOPACK__commonjs__external__node$3a$fs__["promises"].writeFile(tmpFile, JSON.stringify(dbCache, null, 2), 'utf8');
    await __TURBOPACK__commonjs__external__node$3a$fs__["promises"].rename(tmpFile, DB_FILE);
    if (dbDirty) {
        await flushDbToDisk();
    }
}
function scheduleDbFlush() {
    dbDirty = true;
    if (flushTimer) return;
    flushTimer = setTimeout(()=>{
        flushTimer = null;
        flushChain = flushChain.then(()=>flushDbToDisk()).catch(()=>{
            dbDirty = true;
        });
    }, 25);
}
function writeDb(next) {
    dbCache = next;
    scheduleDbFlush();
}
async function updateDb(mutator) {
    if (shouldUseNetlifyBlobs()) {
        return updateDbInNetlify(mutator);
    }
    let result = null;
    mutationQueue = mutationQueue.then(async ()=>{
        const db = await readDb();
        mutator(db);
        writeDb(db);
        result = db;
    });
    await mutationQueue;
    if (!result) {
        throw new Error('Database update failed');
    }
    return result;
}
const platformStore = {
    sessionCookieName: SESSION_COOKIE_NAME,
    createSessionToken,
    verifySessionToken,
    async health () {
        const db = await readDb();
        return {
            status: 'ok',
            users: db.users.length,
            otpChallenges: db.otpChallenges.length,
            products: db.products.length,
            listings: db.listings.length,
            buyers: db.buyers.length,
            prices: db.prices.length,
            posts: db.posts.length,
            orders: db.orders.length,
            payments: db.payments.length,
            predictions: db.predictions.length,
            walletFundingRequests: db.walletFundingRequests.length,
            negotiations: db.negotiations.length,
            agriTrustForms: db.agriTrustForms.length,
            agriTrustSubmissions: db.agriTrustSubmissions.length,
            fieldReports: db.fieldReports.length,
            communicationGroups: db.communicationGroups.length,
            communicationGroupMessages: db.communicationGroupMessages.length
        };
    },
    async login (email, password, role) {
        const db = await readDb();
        const normalizedEmail = normalizeEmail(email);
        const found = db.users.find((u)=>normalizeEmail(u.email) === normalizedEmail);
        if (!found) return null;
        if (role && found.role !== role) return null;
        if (!verifyPassword(password, found.passwordHash)) return null;
        if (found.status === 'suspended') return null;
        return toSafeUser(found);
    },
    async signup (name, email, password, role = 'user') {
        let created = null;
        await updateDb((db)=>{
            const normalizedEmail = normalizeEmail(email);
            if (db.users.some((u)=>normalizeEmail(u.email) === normalizedEmail)) {
                throw new Error('User with this email already exists.');
            }
            const next = createAppUser(name, normalizedEmail, role, hashPassword(password));
            db.users.push(next);
            created = toSafeUser(next);
            ensureWorkspaceProfile(db, created);
            if (created?.role === 'buyer') {
                ensureBuyerProfile(db, created);
            }
        });
        if (!created) {
            throw new Error('User creation failed');
        }
        return created;
    },
    async requestOtp (input) {
        const normalizedEmail = normalizeEmail(input.email);
        const normalizedRole = input.role;
        const normalizedName = input.name ? normalizeName(input.name) : undefined;
        const password = input.password.trim();
        if (!normalizedEmail || !password) {
            throw new Error('Email and password are required.');
        }
        if (input.purpose === 'signup') {
            if (!normalizedName) {
                throw new Error('Name is required for signup.');
            }
            if (normalizedRole === 'admin') {
                throw new Error('Admin signup is invitation-only. Use an existing admin to create admin accounts.');
            }
        }
        let dispatch = null;
        await updateDb((db)=>{
            pruneOtpChallenges(db);
            const existingUser = db.users.find((u)=>normalizeEmail(u.email) === normalizedEmail);
            if (input.purpose === 'login') {
                if (!existingUser) {
                    throw new Error('Invalid email, password, or role.');
                }
                if (existingUser.role !== normalizedRole) {
                    throw new Error(`This account is registered as ${existingUser.role === 'user' ? 'farmer' : existingUser.role}. Select the correct role.`);
                }
                if (existingUser.status === 'suspended') {
                    throw new Error('This account is suspended and cannot sign in.');
                }
                if (!verifyPassword(password, existingUser.passwordHash)) {
                    throw new Error('Invalid email, password, or role.');
                }
            } else if (existingUser) {
                throw new Error('User with this email already exists.');
            }
            const otp = generateOtp();
            const timestamp = nowIso();
            const resendAvailableAt = new Date(Date.now() + OTP_RESEND_COOLDOWN_SECONDS * 1000).toISOString();
            const expiresAt = new Date(Date.now() + OTP_TTL_MINUTES * 60 * 1000).toISOString();
            const existingChallenge = db.otpChallenges.find((challenge)=>challenge.email === normalizedEmail && challenge.role === normalizedRole && challenge.purpose === input.purpose);
            if (existingChallenge) {
                if (isExpired(existingChallenge.expiresAt)) {
                    db.otpChallenges = db.otpChallenges.filter((challenge)=>challenge.id !== existingChallenge.id);
                } else {
                    const waitSeconds = secondsUntil(existingChallenge.resendAvailableAt);
                    if (waitSeconds > 0) {
                        throw new Error(`Please wait ${waitSeconds} seconds before requesting another OTP.`);
                    }
                    if (existingChallenge.sendCount >= OTP_SEND_LIMIT) {
                        throw new Error('OTP request limit reached for this session. Wait for the current code to expire or use the latest code already sent.');
                    }
                    existingChallenge.name = normalizedName ?? existingUser?.name ?? existingChallenge.name;
                    existingChallenge.pendingPasswordHash = input.purpose === 'signup' ? hashPassword(password) : undefined;
                    existingChallenge.otpHash = hashOtp(existingChallenge.id, otp);
                    existingChallenge.attempts = 0;
                    existingChallenge.lastSentAt = timestamp;
                    existingChallenge.resendAvailableAt = resendAvailableAt;
                    existingChallenge.sendCount += 1;
                    existingChallenge.updatedAt = timestamp;
                    dispatch = {
                        challenge: createOtpDispatch(existingChallenge),
                        otpCode: otp,
                        email: normalizedEmail
                    };
                    return;
                }
            }
            const challengeId = id('otp');
            const challenge = {
                id: challengeId,
                purpose: input.purpose,
                email: normalizedEmail,
                role: normalizedRole,
                name: normalizedName ?? existingUser?.name,
                pendingPasswordHash: input.purpose === 'signup' ? hashPassword(password) : undefined,
                otpHash: hashOtp(challengeId, otp),
                expiresAt,
                resendAvailableAt,
                lastSentAt: timestamp,
                sendCount: 1,
                attempts: 0,
                createdAt: timestamp,
                updatedAt: timestamp
            };
            db.otpChallenges = db.otpChallenges.filter((entry)=>!(entry.email === normalizedEmail && entry.role === normalizedRole && entry.purpose === input.purpose));
            db.otpChallenges.unshift(challenge);
            dispatch = {
                challenge: createOtpDispatch(challenge),
                otpCode: otp,
                email: normalizedEmail
            };
        });
        if (!dispatch) {
            throw new Error('Failed to create verification code.');
        }
        return dispatch;
    },
    async deleteOtpChallenge (requestToken) {
        await updateDb((db)=>{
            db.otpChallenges = db.otpChallenges.filter((challenge)=>challenge.id !== requestToken);
        });
    },
    async verifySignupOtp (requestToken, otp) {
        const normalizedOtp = otp.trim();
        if (!requestToken || !normalizedOtp) {
            throw new Error('Request token and OTP are required.');
        }
        let created = null;
        await updateDb((db)=>{
            pruneOtpChallenges(db);
            const challengeIndex = db.otpChallenges.findIndex((challenge)=>challenge.id === requestToken && challenge.purpose === 'signup');
            if (challengeIndex < 0) {
                throw new Error('Verification session expired. Request a new OTP.');
            }
            const challenge = db.otpChallenges[challengeIndex];
            if (challenge.attempts >= OTP_ATTEMPT_LIMIT || isExpired(challenge.expiresAt)) {
                db.otpChallenges.splice(challengeIndex, 1);
                throw new Error('OTP expired. Request a new verification code.');
            }
            if (!verifyOtp(challenge.id, normalizedOtp, challenge.otpHash)) {
                challenge.attempts += 1;
                challenge.updatedAt = nowIso();
                if (challenge.attempts >= OTP_ATTEMPT_LIMIT) {
                    db.otpChallenges.splice(challengeIndex, 1);
                }
                throw new Error('Invalid OTP.');
            }
            if (!challenge.name || !challenge.pendingPasswordHash) {
                db.otpChallenges.splice(challengeIndex, 1);
                throw new Error('Verification session is invalid. Request a new OTP.');
            }
            if (db.users.some((user)=>normalizeEmail(user.email) === challenge.email)) {
                db.otpChallenges.splice(challengeIndex, 1);
                throw new Error('User with this email already exists.');
            }
            const next = createAppUser(challenge.name, challenge.email, challenge.role, challenge.pendingPasswordHash);
            db.users.push(next);
            created = toSafeUser(next);
            ensureWorkspaceProfile(db, created);
            if (created.role === 'buyer') {
                ensureBuyerProfile(db, created);
            }
            db.otpChallenges.splice(challengeIndex, 1);
        });
        if (!created) {
            throw new Error('Signup verification failed.');
        }
        return created;
    },
    async verifyLoginOtp (requestToken, otp) {
        const normalizedOtp = otp.trim();
        if (!requestToken || !normalizedOtp) {
            throw new Error('Request token and OTP are required.');
        }
        let signedIn = null;
        await updateDb((db)=>{
            pruneOtpChallenges(db);
            const challengeIndex = db.otpChallenges.findIndex((challenge)=>challenge.id === requestToken && challenge.purpose === 'login');
            if (challengeIndex < 0) {
                throw new Error('Verification session expired. Request a new OTP.');
            }
            const challenge = db.otpChallenges[challengeIndex];
            if (challenge.attempts >= OTP_ATTEMPT_LIMIT || isExpired(challenge.expiresAt)) {
                db.otpChallenges.splice(challengeIndex, 1);
                throw new Error('OTP expired. Request a new verification code.');
            }
            if (!verifyOtp(challenge.id, normalizedOtp, challenge.otpHash)) {
                challenge.attempts += 1;
                challenge.updatedAt = nowIso();
                if (challenge.attempts >= OTP_ATTEMPT_LIMIT) {
                    db.otpChallenges.splice(challengeIndex, 1);
                }
                throw new Error('Invalid OTP.');
            }
            const user = db.users.find((entry)=>normalizeEmail(entry.email) === challenge.email);
            if (!user || user.role !== challenge.role) {
                db.otpChallenges.splice(challengeIndex, 1);
                throw new Error('Account not found for this verification request.');
            }
            if (user.status === 'suspended') {
                db.otpChallenges.splice(challengeIndex, 1);
                throw new Error('This account is suspended and cannot sign in.');
            }
            signedIn = toSafeUser(user);
            db.otpChallenges.splice(challengeIndex, 1);
        });
        if (!signedIn) {
            throw new Error('Login verification failed.');
        }
        return signedIn;
    },
    async findUserByEmail (email) {
        const db = await readDb();
        const found = db.users.find((u)=>normalizeEmail(u.email) === normalizeEmail(email));
        return found ? toSafeUser(found) : null;
    },
    async updateUser (email, updates) {
        let updated = null;
        await updateDb((db)=>{
            const idx = db.users.findIndex((u)=>normalizeEmail(u.email) === normalizeEmail(email));
            if (idx < 0) return;
            db.users[idx] = {
                ...db.users[idx],
                ...updates,
                updatedAt: nowIso()
            };
            updated = toSafeUser(db.users[idx]);
            if (updated) {
                ensureWorkspaceProfile(db, updated);
            }
        });
        return updated;
    },
    async deleteUser (email) {
        let deleted = false;
        await updateDb((db)=>{
            const normalized = normalizeEmail(email);
            const before = db.users.length;
            db.users = db.users.filter((user)=>normalizeEmail(user.email) !== normalized);
            db.profiles = db.profiles.filter((profile)=>normalizeEmail(profile.userEmail) !== normalized);
            db.buyers = db.buyers.filter((buyer)=>normalizeEmail(buyer.contact ?? '') !== normalized);
            db.notifications = db.notifications.filter((notification)=>normalizeEmail(notification.userEmail) !== normalized);
            db.orders = db.orders.filter((order)=>normalizeEmail(order.userEmail) !== normalized);
            db.shipments = db.shipments.filter((shipment)=>normalizeEmail(shipment.userEmail) !== normalized);
            db.invoices = db.invoices.filter((invoice)=>normalizeEmail(invoice.userEmail) !== normalized);
            db.walletFundingRequests = db.walletFundingRequests.filter((request)=>normalizeEmail(request.userEmail) !== normalized);
            db.negotiations = db.negotiations.filter((item)=>normalizeEmail(item.creatorEmail) !== normalized && normalizeEmail(item.targetEmail ?? '') !== normalized);
            db.agriTrustForms = db.agriTrustForms.filter((form)=>normalizeEmail(form.createdBy) !== normalized && normalizeEmail(form.targetEmail ?? '') !== normalized);
            db.agriTrustSubmissions = db.agriTrustSubmissions.filter((submission)=>normalizeEmail(submission.userEmail) !== normalized);
            db.fieldReports = db.fieldReports.filter((report)=>normalizeEmail(report.userEmail) !== normalized);
            db.communicationGroups = db.communicationGroups.map((group)=>({
                    ...group,
                    memberEmails: group.memberEmails.filter((memberEmail)=>normalizeEmail(memberEmail) !== normalized)
                })).filter((group)=>normalizeEmail(group.createdBy) !== normalized && group.memberEmails.length > 0);
            db.communicationGroupMessages = db.communicationGroupMessages.filter((message)=>normalizeEmail(message.senderEmail) !== normalized);
            deleted = db.users.length !== before;
        });
        return deleted;
    },
    async listUsers () {
        const db = await readDb();
        return db.users.map(toSafeUser);
    },
    async listLocations () {
        const db = await readDb();
        const locations = new Set();
        for (const listing of db.listings){
            if (listing.location?.trim()) locations.add(listing.location.trim());
        }
        for (const buyer of db.buyers){
            const city = buyer.location?.split(',')[0]?.trim();
            if (city) locations.add(city);
        }
        for (const price of db.prices){
            const city = price.market?.split(' ')[0]?.trim();
            if (city) locations.add(city);
        }
        return [
            ...locations
        ].sort((a, b)=>a.localeCompare(b));
    },
    async listProducts () {
        const db = await readDb();
        return [
            ...db.products
        ].sort((a, b)=>(b.rating ?? 0) - (a.rating ?? 0) || a.name.localeCompare(b.name));
    },
    async createProduct (input) {
        let created = null;
        await updateDb((db)=>{
            const timestamp = nowIso();
            const next = {
                id: id('prd'),
                ...input,
                createdAt: timestamp,
                updatedAt: timestamp
            };
            db.products.push(next);
            created = next;
        });
        if (!created) {
            throw new Error('Product creation failed');
        }
        return created;
    },
    async listListings () {
        const db = await readDb();
        return [
            ...db.listings
        ].sort((a, b)=>{
            if (a.status !== b.status) return a.status === 'active' ? -1 : 1;
            return (b.buyerInterestCount ?? 0) - (a.buyerInterestCount ?? 0);
        });
    },
    async createListing (input) {
        let created = null;
        await updateDb((db)=>{
            const timestamp = nowIso();
            const next = {
                id: id('lst'),
                ...input,
                status: 'active',
                createdAt: timestamp,
                updatedAt: timestamp
            };
            db.listings.push(next);
            created = next;
        });
        if (!created) {
            throw new Error('Listing creation failed');
        }
        return created;
    },
    async listBuyers () {
        const db = await readDb();
        return [
            ...db.buyers
        ].sort((a, b)=>Number(Boolean(b.verified)) - Number(Boolean(a.verified)) || (b.trades ?? 0) - (a.trades ?? 0));
    },
    async createBuyer (input) {
        let created = null;
        await updateDb((db)=>{
            const timestamp = nowIso();
            const next = {
                id: id('buy'),
                ...input,
                createdAt: timestamp,
                updatedAt: timestamp
            };
            db.buyers.unshift(next);
            created = next;
        });
        if (!created) throw new Error('Buyer creation failed');
        return created;
    },
    async updateBuyer (idValue, updates) {
        let updated = null;
        await updateDb((db)=>{
            const index = db.buyers.findIndex((buyer)=>buyer.id === idValue);
            if (index < 0) return;
            db.buyers[index] = {
                ...db.buyers[index],
                ...updates,
                updatedAt: nowIso()
            };
            updated = db.buyers[index];
        });
        return updated;
    },
    async deleteBuyer (idValue) {
        let deleted = false;
        await updateDb((db)=>{
            const before = db.buyers.length;
            db.buyers = db.buyers.filter((buyer)=>buyer.id !== idValue);
            deleted = db.buyers.length !== before;
        });
        return deleted;
    },
    async listPrices () {
        const db = await readDb();
        const sorted = [
            ...db.prices
        ].sort((a, b)=>b.timestamp.localeCompare(a.timestamp) || a.commodity.localeCompare(b.commodity));
        return sorted.map((row)=>(0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$server$2f$pricing$2e$ts__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["enrichCommodityPrice"])(row, sorted));
    },
    async addPriceEntry (entry) {
        let created = null;
        await updateDb((db)=>{
            const next = {
                id: id('prc'),
                timestamp: nowIso(),
                ...entry
            };
            db.prices.unshift(next);
            created = next;
        });
        if (!created) {
            throw new Error('Price entry creation failed');
        }
        const history = await readDb();
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$server$2f$pricing$2e$ts__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["enrichCommodityPrice"])(created, history.prices);
    },
    async listPosts () {
        const db = await readDb();
        return [
            ...db.posts
        ].sort((a, b)=>b.createdAt.localeCompare(a.createdAt));
    },
    async createPost (input) {
        let created = null;
        await updateDb((db)=>{
            const timestamp = nowIso();
            const next = {
                id: id('pst'),
                ...input,
                likes: 0,
                comments: 0,
                createdAt: timestamp,
                updatedAt: timestamp
            };
            db.posts.unshift(next);
            created = next;
        });
        if (!created) {
            throw new Error('Post creation failed');
        }
        return created;
    },
    async updatePost (idValue, updates) {
        let updated = null;
        await updateDb((db)=>{
            const index = db.posts.findIndex((post)=>post.id === idValue);
            if (index < 0) return;
            db.posts[index] = {
                ...db.posts[index],
                ...updates,
                updatedAt: nowIso()
            };
            updated = db.posts[index];
        });
        return updated;
    },
    async deletePost (idValue) {
        let deleted = false;
        await updateDb((db)=>{
            const before = db.posts.length;
            db.posts = db.posts.filter((post)=>post.id !== idValue);
            deleted = db.posts.length !== before;
        });
        return deleted;
    },
    async createOrder (input) {
        let created = null;
        await updateDb((db)=>{
            const timestamp = nowIso();
            const totalAmount = input.items.reduce((sum, item)=>sum + item.price * item.quantity, 0);
            const invoiceId = `INV-${Date.now().toString().slice(-6)}-${Math.floor(Math.random() * 90 + 10)}`;
            const shipmentId = `SHP-${Date.now().toString().slice(-6)}-${Math.floor(Math.random() * 90 + 10)}`;
            const next = {
                id: `ORD-${Date.now().toString().slice(-6)}-${Math.floor(Math.random() * 900 + 100)}`,
                userEmail: input.userEmail,
                userName: input.userName,
                items: input.items,
                totalAmount,
                status: 'created',
                paymentStatus: 'initiated',
                invoiceId,
                shipmentId,
                courier: 'AgriFleet',
                shippingAddress: input.shippingAddress,
                createdAt: timestamp,
                updatedAt: timestamp
            };
            db.orders.unshift(next);
            db.invoices.unshift({
                id: invoiceId,
                orderId: next.id,
                userEmail: input.userEmail,
                issuedTo: input.userName,
                issuedBy: 'Farmer Marketplace Commerce',
                lineItems: input.items,
                subtotal: totalAmount,
                taxAmount: 0,
                totalAmount,
                createdAt: timestamp
            });
            db.shipments.unshift({
                id: shipmentId,
                orderId: next.id,
                userEmail: input.userEmail,
                courier: 'AgriFleet',
                origin: 'Platform dispatch node',
                destination: input.shippingAddress,
                routeCode: `RT-${shipmentId.slice(-5)}`,
                vehicleId: 'AGFLEET-01',
                etaHours: 36,
                status: 'processing',
                lastCheckpoint: 'Order created and awaiting payment confirmation',
                checkpoints: [
                    createShipmentCheckpoint('processing', 'Order created and awaiting payment confirmation', 'Platform dispatch node', 'system')
                ],
                deliveryVerified: false,
                createdAt: timestamp,
                updatedAt: timestamp
            });
            db.notifications.unshift(createNotification({
                userEmail: input.userEmail,
                title: 'Order created',
                body: `${next.id} has been created and moved to payment confirmation.`,
                category: 'trade',
                actionHref: '/profile'
            }));
            created = next;
        });
        if (!created) {
            throw new Error('Order creation failed');
        }
        return created;
    },
    async listOrders (userEmail) {
        const db = await readDb();
        const orders = !userEmail ? db.orders : db.orders.filter((order)=>order.userEmail.toLowerCase() === userEmail.toLowerCase());
        return [
            ...orders
        ].sort((a, b)=>b.createdAt.localeCompare(a.createdAt));
    },
    async getOrderById (orderId) {
        const db = await readDb();
        return db.orders.find((order)=>order.id === orderId) ?? null;
    },
    async recordPayment (input) {
        let result = null;
        await updateDb((db)=>{
            const orderIndex = db.orders.findIndex((order)=>order.id === input.orderId);
            if (orderIndex < 0) {
                throw new Error('Order not found');
            }
            const timestamp = nowIso();
            const paymentStatus = input.success === false ? 'failed' : 'paid';
            const payment = {
                id: id('pay'),
                orderId: input.orderId,
                userEmail: input.userEmail,
                amount: input.amount,
                status: paymentStatus,
                method: input.method,
                transactionRef: `TXN-${Date.now().toString().slice(-8)}`,
                createdAt: timestamp,
                updatedAt: timestamp
            };
            db.payments.unshift(payment);
            const shipmentIndex = db.shipments.findIndex((shipment)=>shipment.orderId === input.orderId);
            if (shipmentIndex >= 0) {
                const shipmentStatus = paymentStatus === 'paid' ? 'packed' : 'processing';
                const checkpointNote = paymentStatus === 'paid' ? 'Payment cleared and packed for dispatch' : 'Payment failed, awaiting retry';
                db.shipments[shipmentIndex] = {
                    ...db.shipments[shipmentIndex],
                    status: shipmentStatus,
                    etaHours: paymentStatus === 'paid' ? Math.max(18, db.shipments[shipmentIndex].etaHours - 8) : db.shipments[shipmentIndex].etaHours,
                    lastCheckpoint: checkpointNote,
                    checkpoints: [
                        createShipmentCheckpoint(shipmentStatus, checkpointNote, db.shipments[shipmentIndex].origin, 'payment-system'),
                        ...db.shipments[shipmentIndex].checkpoints ?? []
                    ],
                    updatedAt: timestamp
                };
            }
            db.orders[orderIndex] = {
                ...db.orders[orderIndex],
                paymentStatus,
                status: paymentStatus === 'paid' ? 'paid' : 'failed',
                updatedAt: timestamp
            };
            db.notifications.unshift(createNotification({
                userEmail: input.userEmail,
                title: paymentStatus === 'paid' ? 'Payment successful' : 'Payment failed',
                body: paymentStatus === 'paid' ? `Order ${input.orderId} is confirmed and shipment preparation has started.` : `Order ${input.orderId} needs a new payment attempt.`,
                category: paymentStatus === 'paid' ? 'finance' : 'system',
                actionHref: '/profile'
            }));
            result = {
                order: db.orders[orderIndex],
                payment
            };
        });
        if (!result) {
            throw new Error('Payment recording failed');
        }
        return result;
    },
    async listPayments (userEmail) {
        const db = await readDb();
        const payments = !userEmail ? db.payments : db.payments.filter((payment)=>payment.userEmail.toLowerCase() === userEmail.toLowerCase());
        return [
            ...payments
        ].sort((a, b)=>b.createdAt.localeCompare(a.createdAt));
    },
    async listInvoices (userEmail) {
        const db = await readDb();
        const invoices = userEmail ? db.invoices.filter((invoice)=>invoice.userEmail.toLowerCase() === userEmail.toLowerCase()) : db.invoices;
        return [
            ...invoices
        ].sort((a, b)=>b.createdAt.localeCompare(a.createdAt));
    },
    async listShipments (userEmail) {
        const db = await readDb();
        const shipments = userEmail ? db.shipments.filter((shipment)=>shipment.userEmail.toLowerCase() === userEmail.toLowerCase()) : db.shipments;
        return [
            ...shipments
        ].sort((a, b)=>b.updatedAt.localeCompare(a.updatedAt));
    },
    async updateShipment (idValue, updates) {
        let updated = null;
        await updateDb((db)=>{
            const index = db.shipments.findIndex((shipment)=>shipment.id === idValue);
            if (index < 0) return;
            const current = db.shipments[index];
            const nextStatus = updates.status ?? current.status;
            const nextCheckpoint = updates.lastCheckpoint ?? current.lastCheckpoint;
            const nextLocation = updates.checkpointLocation ?? current.destination;
            db.shipments[index] = {
                ...current,
                ...updates,
                status: nextStatus,
                lastCheckpoint: nextCheckpoint,
                deliveryVerified: nextStatus === 'delivered' ? current.deliveryVerified : false,
                checkpoints: current.status !== nextStatus || current.lastCheckpoint !== nextCheckpoint ? [
                    createShipmentCheckpoint(nextStatus, nextCheckpoint, nextLocation, updates.updatedBy),
                    ...current.checkpoints ?? []
                ] : current.checkpoints ?? [],
                updatedAt: nowIso()
            };
            updated = db.shipments[index];
            db.notifications.unshift(createNotification({
                userEmail: db.shipments[index].userEmail,
                title: 'Shipment updated',
                body: `${db.shipments[index].id} is now ${db.shipments[index].status.replace(/_/g, ' ')}.`,
                category: 'ops',
                actionHref: '/logistics-hub'
            }));
        });
        return updated;
    },
    async acknowledgeShipment (idValue, actorEmail) {
        let updated = null;
        await updateDb((db)=>{
            const index = db.shipments.findIndex((shipment)=>shipment.id === idValue && shipment.userEmail.toLowerCase() === actorEmail.toLowerCase());
            if (index < 0) return;
            const current = db.shipments[index];
            const timestamp = nowIso();
            db.shipments[index] = {
                ...current,
                status: 'delivered',
                lastCheckpoint: 'Delivery received and acknowledged by verified customer',
                deliveryVerified: true,
                acknowledgedBy: actorEmail,
                acknowledgedAt: timestamp,
                checkpoints: [
                    createShipmentCheckpoint('delivered', 'Delivery received and acknowledged by verified customer', current.destination, actorEmail),
                    ...current.checkpoints ?? []
                ],
                updatedAt: timestamp
            };
            updated = db.shipments[index];
            db.notifications.unshift(createNotification({
                userEmail: actorEmail,
                title: 'Delivery acknowledged',
                body: `${current.id} has been marked as received by the customer.`,
                category: 'ops',
                actionHref: '/profile'
            }));
        });
        return updated;
    },
    async logPrediction (input) {
        let created = null;
        await updateDb((db)=>{
            const next = {
                id: id('pred'),
                type: input.type,
                userEmail: input.userEmail,
                inputSummary: input.inputSummary,
                outputSummary: input.outputSummary,
                createdAt: nowIso()
            };
            db.predictions.unshift(next);
            created = next;
        });
        if (!created) {
            throw new Error('Prediction logging failed');
        }
        return created;
    },
    async listPredictions (userEmail) {
        const db = await readDb();
        const predictions = userEmail ? db.predictions.filter((prediction)=>prediction.userEmail.toLowerCase() === userEmail.toLowerCase()) : db.predictions;
        return [
            ...predictions
        ].sort((a, b)=>b.createdAt.localeCompare(a.createdAt));
    },
    async listBuyerInteractions (userEmail) {
        const db = await readDb();
        const interactions = userEmail ? db.buyerInteractions.filter((interaction)=>interaction.senderEmail.toLowerCase() === userEmail.toLowerCase()) : db.buyerInteractions;
        return [
            ...interactions
        ].sort((a, b)=>b.createdAt.localeCompare(a.createdAt));
    },
    async createBuyerInteraction (input) {
        let created = null;
        await updateDb((db)=>{
            const next = {
                id: id('int'),
                ...input,
                status: input.status ?? (input.mode === 'call' ? 'scheduled' : 'pending'),
                createdAt: nowIso()
            };
            db.buyerInteractions.unshift(next);
            created = next;
        });
        if (!created) throw new Error('Interaction creation failed');
        return created;
    },
    async listPriceAlerts (userEmail) {
        const db = await readDb();
        return userEmail ? db.priceAlerts.filter((alert)=>alert.userEmail.toLowerCase() === userEmail.toLowerCase()) : db.priceAlerts;
    },
    async createPriceAlert (input) {
        let created = null;
        await updateDb((db)=>{
            const next = {
                id: id('alt'),
                ...input,
                createdAt: nowIso()
            };
            db.priceAlerts.unshift(next);
            created = next;
        });
        if (!created) throw new Error('Price alert creation failed');
        return created;
    },
    async listFinanceTransactions (userEmail) {
        const db = await readDb();
        const transactions = userEmail ? db.financeTransactions.filter((transaction)=>transaction.userEmail.toLowerCase() === userEmail.toLowerCase()) : db.financeTransactions;
        return [
            ...transactions
        ].sort((a, b)=>b.createdAt.localeCompare(a.createdAt));
    },
    async createFinanceTransaction (input) {
        let created = null;
        await updateDb((db)=>{
            const next = {
                id: id('txn'),
                ...input,
                status: input.status ?? 'completed',
                createdAt: nowIso()
            };
            db.financeTransactions.unshift(next);
            db.notifications.unshift(createNotification({
                userEmail: input.userEmail,
                title: input.direction === 'in' ? 'Wallet credited' : 'Wallet debited',
                body: `${input.type} for INR ${input.amount.toLocaleString()} has been recorded.`,
                category: 'finance',
                actionHref: '/finance'
            }));
            created = next;
        });
        if (!created) throw new Error('Transaction creation failed');
        return created;
    },
    async listWalletFundingRequests (userEmail) {
        const db = await readDb();
        const items = userEmail ? db.walletFundingRequests.filter((request)=>request.userEmail.toLowerCase() === userEmail.toLowerCase()) : db.walletFundingRequests;
        return [
            ...items
        ].sort((a, b)=>b.updatedAt.localeCompare(a.updatedAt));
    },
    async createWalletFundingRequest (input) {
        let created = null;
        await updateDb((db)=>{
            const timestamp = nowIso();
            const next = {
                id: id('wfr'),
                ...input,
                status: 'requested',
                createdAt: timestamp,
                updatedAt: timestamp
            };
            db.walletFundingRequests.unshift(next);
            notifyAdmins(db, {
                title: 'Wallet funding request pending',
                body: `${input.userName} requested INR ${input.amount.toLocaleString()} wallet funding approval.`,
                category: 'finance',
                actionHref: '/admin/financials'
            });
            created = next;
        });
        if (!created) throw new Error('Wallet funding request failed');
        return created;
    },
    async updateWalletFundingRequest (idValue, updates) {
        let updated = null;
        await updateDb((db)=>{
            const index = db.walletFundingRequests.findIndex((request)=>request.id === idValue);
            if (index < 0) return;
            db.walletFundingRequests[index] = {
                ...db.walletFundingRequests[index],
                ...updates,
                updatedAt: nowIso()
            };
            updated = db.walletFundingRequests[index];
            if (updated.status === 'verified') {
                db.financeTransactions.unshift({
                    id: id('txn'),
                    userEmail: updated.userEmail,
                    type: 'Wallet funding approved by admin',
                    amount: updated.amount,
                    direction: 'in',
                    method: 'manual-bank-transfer',
                    status: 'verified',
                    createdAt: nowIso()
                });
                db.notifications.unshift(createNotification({
                    userEmail: updated.userEmail,
                    title: 'Wallet funding verified',
                    body: `Admin verified INR ${updated.amount.toLocaleString()} for your wallet.`,
                    category: 'finance',
                    actionHref: '/finance'
                }));
            }
        });
        return updated;
    },
    async listLoanApplications (userEmail) {
        const db = await readDb();
        const loans = userEmail ? db.loanApplications.filter((loan)=>loan.userEmail.toLowerCase() === userEmail.toLowerCase()) : db.loanApplications;
        return [
            ...loans
        ].sort((a, b)=>b.updatedAt.localeCompare(a.updatedAt));
    },
    async createLoanApplication (input) {
        let created = null;
        await updateDb((db)=>{
            const profile = db.profiles.find((profileRow)=>profileRow.userEmail.toLowerCase() === input.userEmail.toLowerCase());
            if (!profile || profile.trustScore < 90) {
                throw new Error('Only Agri Trust verified customers can request loans.');
            }
            const timestamp = nowIso();
            const risk = input.amount >= 500000 ? 'High' : input.amount >= 200000 ? 'Medium' : 'Low';
            const next = {
                id: `LOAN-${Date.now().toString().slice(-6)}`,
                ...input,
                risk: input.risk ?? risk,
                status: input.status ?? 'Pending',
                createdAt: timestamp,
                updatedAt: timestamp
            };
            db.loanApplications.unshift(next);
            db.notifications.unshift(createNotification({
                userEmail: input.userEmail,
                title: 'Loan application submitted',
                body: `${next.id} for INR ${next.amount.toLocaleString()} is now under review.`,
                category: 'finance',
                actionHref: '/finance'
            }));
            created = next;
        });
        if (!created) throw new Error('Loan application failed');
        return created;
    },
    async updateLoanApplication (idValue, updates) {
        let updated = null;
        await updateDb((db)=>{
            const index = db.loanApplications.findIndex((loan)=>loan.id === idValue);
            if (index < 0) return;
            db.loanApplications[index] = {
                ...db.loanApplications[index],
                ...updates,
                updatedAt: nowIso()
            };
            updated = db.loanApplications[index];
        });
        return updated;
    },
    async listNegotiations (userEmail) {
        const db = await readDb();
        const items = userEmail ? db.negotiations.filter((item)=>item.creatorEmail.toLowerCase() === userEmail.toLowerCase() || (item.targetEmail ? item.targetEmail.toLowerCase() === userEmail.toLowerCase() : false)) : db.negotiations;
        return [
            ...items
        ].sort((a, b)=>b.updatedAt.localeCompare(a.updatedAt));
    },
    async createNegotiation (input) {
        let created = null;
        await updateDb((db)=>{
            const timestamp = nowIso();
            const next = {
                id: id('neg'),
                ...input,
                status: input.status ?? 'awaiting-response',
                createdAt: timestamp,
                updatedAt: timestamp
            };
            db.negotiations.unshift(next);
            if (input.targetEmail) {
                db.notifications.unshift(createNotification({
                    userEmail: input.targetEmail,
                    title: input.mode === 'form' ? 'Information request received' : 'Negotiation update received',
                    body: `${input.creatorName} shared ${input.mode} details for ${input.commodity}.`,
                    category: 'trade',
                    actionHref: '/buyers'
                }));
            }
            created = next;
        });
        if (!created) throw new Error('Negotiation creation failed');
        return created;
    },
    async updateNegotiation (idValue, updates) {
        let updated = null;
        await updateDb((db)=>{
            const index = db.negotiations.findIndex((item)=>item.id === idValue);
            if (index < 0) return;
            db.negotiations[index] = {
                ...db.negotiations[index],
                ...updates,
                updatedAt: nowIso()
            };
            updated = db.negotiations[index];
        });
        return updated;
    },
    async listAgriTrustForms (userEmail, role) {
        const db = await readDb();
        const items = db.agriTrustForms.filter((form)=>{
            if (!userEmail && !role) return true;
            if (form.targetEmail && userEmail) return form.targetEmail.toLowerCase() === userEmail.toLowerCase();
            if (role === 'admin') return false;
            if (role) return form.audienceRole === 'all' || form.audienceRole === role;
            return true;
        });
        return [
            ...items
        ].sort((a, b)=>b.updatedAt.localeCompare(a.updatedAt));
    },
    async createAgriTrustForm (input) {
        let created = null;
        await updateDb((db)=>{
            const timestamp = nowIso();
            const normalizedTargetEmail = input.targetEmail ? normalizeEmail(input.targetEmail) : null;
            const targetRole = normalizedTargetEmail ? db.users.find((user)=>normalizeEmail(user.email) === normalizedTargetEmail)?.role : null;
            if (targetRole === 'admin') {
                throw new Error('Admin accounts cannot receive AgriTrust customer forms.');
            }
            const next = {
                id: id('atf'),
                ...input,
                status: input.status ?? 'open',
                createdAt: timestamp,
                updatedAt: timestamp
            };
            db.agriTrustForms.unshift(next);
            const targets = db.users.filter((user)=>{
                if (next.targetEmail) return user.email.toLowerCase() === next.targetEmail.toLowerCase();
                if (user.role === 'admin') return false;
                return next.audienceRole === 'all' || user.role === next.audienceRole;
            });
            for (const target of targets){
                db.notifications.unshift(createNotification({
                    userEmail: target.email,
                    title: 'AgriTrust form received',
                    body: `${next.title} requires your response in the profile vault.`,
                    category: 'system',
                    actionHref: '/profile'
                }));
            }
            created = next;
        });
        if (!created) throw new Error('AgriTrust form creation failed');
        return created;
    },
    async listAgriTrustSubmissions (userEmail) {
        const db = await readDb();
        const items = userEmail ? db.agriTrustSubmissions.filter((submission)=>submission.userEmail.toLowerCase() === userEmail.toLowerCase()) : db.agriTrustSubmissions;
        return [
            ...items
        ].sort((a, b)=>b.updatedAt.localeCompare(a.updatedAt));
    },
    async submitAgriTrustForm (input) {
        let created = null;
        await updateDb((db)=>{
            if (input.role === 'admin') {
                throw new Error('Admin accounts cannot submit AgriTrust customer forms.');
            }
            const timestamp = nowIso();
            const existingIndex = db.agriTrustSubmissions.findIndex((submission)=>submission.formId === input.formId && submission.userEmail.toLowerCase() === input.userEmail.toLowerCase());
            const next = {
                id: existingIndex >= 0 ? db.agriTrustSubmissions[existingIndex].id : id('ats'),
                ...input,
                status: 'submitted',
                createdAt: existingIndex >= 0 ? db.agriTrustSubmissions[existingIndex].createdAt : timestamp,
                updatedAt: timestamp
            };
            if (existingIndex >= 0) {
                db.agriTrustSubmissions[existingIndex] = next;
            } else {
                db.agriTrustSubmissions.unshift(next);
            }
            notifyAdmins(db, {
                title: 'AgriTrust submission received',
                body: `${input.userName} submitted a trust form for verification review.`,
                category: 'system',
                actionHref: '/admin/trust'
            });
            created = next;
        });
        if (!created) throw new Error('AgriTrust form submission failed');
        return created;
    },
    async reviewAgriTrustSubmission (idValue, updates) {
        let updated = null;
        await updateDb((db)=>{
            const index = db.agriTrustSubmissions.findIndex((submission)=>submission.id === idValue);
            if (index < 0) return;
            db.agriTrustSubmissions[index] = {
                ...db.agriTrustSubmissions[index],
                status: updates.status,
                reviewedBy: updates.reviewedBy,
                reviewedAt: nowIso(),
                updatedAt: nowIso()
            };
            updated = db.agriTrustSubmissions[index];
        });
        return updated;
    },
    async listAgriTrustProfiles (viewerRole) {
        const db = await readDb();
        const items = db.profiles.filter((profile)=>{
            if (viewerRole === 'admin') return profile.role !== 'admin';
            if (viewerRole === 'buyer') return profile.role === 'user';
            return false;
        });
        return [
            ...items
        ].map(normalizeWorkspaceProfileForRole).sort((a, b)=>b.updatedAt.localeCompare(a.updatedAt));
    },
    async listFieldReports (userEmail) {
        const db = await readDb();
        const items = userEmail ? db.fieldReports.filter((report)=>report.userEmail.toLowerCase() === userEmail.toLowerCase()) : db.fieldReports;
        return [
            ...items
        ].sort((a, b)=>b.updatedAt.localeCompare(a.updatedAt));
    },
    async createFieldReport (input) {
        let created = null;
        await updateDb((db)=>{
            const timestamp = nowIso();
            const rewardPoints = input.reportType === 'mandi-price' ? 12 : input.reportType === 'weather' ? 8 : 10;
            const rewardAmount = rewardPoints * 10;
            const next = {
                id: id('frp'),
                ...input,
                rewardPoints,
                rewardAmount,
                status: 'submitted',
                createdAt: timestamp,
                updatedAt: timestamp
            };
            db.fieldReports.unshift(next);
            notifyAdmins(db, {
                title: 'Farmer field report submitted',
                body: `${input.userName} submitted ${input.reportType} data from ${input.city}.`,
                category: 'ops',
                actionHref: '/admin/trust'
            });
            created = next;
        });
        if (!created) throw new Error('Field report creation failed');
        return created;
    },
    async reviewFieldReport (idValue, updates) {
        let updated = null;
        await updateDb((db)=>{
            const index = db.fieldReports.findIndex((report)=>report.id === idValue);
            if (index < 0) return;
            const current = db.fieldReports[index];
            const timestamp = nowIso();
            db.fieldReports[index] = {
                ...current,
                status: updates.status,
                verifiedBy: updates.verifiedBy,
                verifiedAt: timestamp,
                updatedAt: timestamp
            };
            updated = db.fieldReports[index];
            if (updates.status === 'verified') {
                const userIndex = db.users.findIndex((user)=>user.email.toLowerCase() === current.userEmail.toLowerCase());
                if (userIndex >= 0) {
                    db.users[userIndex] = {
                        ...db.users[userIndex],
                        aiCredits: db.users[userIndex].aiCredits + current.rewardPoints,
                        updatedAt: timestamp
                    };
                }
                const profileIndex = db.profiles.findIndex((profile)=>profile.userEmail.toLowerCase() === current.userEmail.toLowerCase());
                if (profileIndex >= 0) {
                    db.profiles[profileIndex] = {
                        ...db.profiles[profileIndex],
                        trustScore: Math.min(99, db.profiles[profileIndex].trustScore + Math.ceil(current.rewardPoints / 2)),
                        trustLabel: buildTrustLabel(Math.min(99, db.profiles[profileIndex].trustScore + Math.ceil(current.rewardPoints / 2))),
                        updatedAt: timestamp
                    };
                }
                db.financeTransactions.unshift({
                    id: id('txn'),
                    userEmail: current.userEmail,
                    type: 'Field intelligence reward',
                    amount: current.rewardAmount,
                    direction: 'in',
                    method: 'wallet-grant',
                    status: 'verified',
                    createdAt: timestamp
                });
                db.notifications.unshift(createNotification({
                    userEmail: current.userEmail,
                    title: 'Field report verified',
                    body: `Your report earned INR ${current.rewardAmount.toLocaleString()} and ${current.rewardPoints} Agri credits.`,
                    category: 'finance',
                    actionHref: '/finance'
                }));
            }
        });
        return updated;
    },
    async verifyAgriTrustProfile (input) {
        let updated = null;
        await updateDb((db)=>{
            const index = db.profiles.findIndex((profile)=>profile.userEmail.toLowerCase() === input.userEmail.toLowerCase());
            if (index < 0) return;
            const current = db.profiles[index];
            if (current.role === 'admin') {
                throw new Error('Admin accounts are validators and cannot be AgriTrust verification targets.');
            }
            if (current.role !== input.role) {
                throw new Error(`Profile role mismatch. Expected ${current.role}, received ${input.role}.`);
            }
            if (input.verifiedByRole === 'buyer' && current.role !== 'user') {
                throw new Error('Buyers can validate farmers only.');
            }
            const timestamp = nowIso();
            const verificationEntry = {
                status: 'verified',
                label: input.label,
                submittedAt: input.role === 'user' ? current.verification.farmer?.submittedAt ?? timestamp : current.verification.buyer?.submittedAt ?? timestamp,
                verifiedAt: timestamp,
                verifiedByRole: input.verifiedByRole,
                verifiedByEmail: input.verifiedByEmail
            };
            const next = {
                ...current,
                verification: {
                    farmer: input.role === 'user' ? verificationEntry : current.verification.farmer,
                    buyer: input.role === 'buyer' ? verificationEntry : current.verification.buyer,
                    admin: current.verification.admin
                },
                updatedAt: timestamp
            };
            db.profiles[index] = normalizeWorkspaceProfileForRole(next);
            const normalizedNext = db.profiles[index];
            if (normalizedNext.role === 'buyer') {
                const buyerIndex = db.buyers.findIndex((buyer)=>normalizeEmail(buyer.contact ?? '') === normalizedNext.userEmail.toLowerCase());
                if (buyerIndex >= 0) {
                    db.buyers[buyerIndex] = {
                        ...db.buyers[buyerIndex],
                        name: normalizedNext.buyerProfile?.businessName || normalizedNext.displayName,
                        location: [
                            normalizedNext.city,
                            normalizedNext.state
                        ].filter(Boolean).join(', '),
                        verified: true,
                        status: 'Verified',
                        updatedAt: timestamp
                    };
                } else {
                    db.buyers.unshift({
                        id: id('buy'),
                        name: normalizedNext.buyerProfile?.businessName || normalizedNext.displayName,
                        location: [
                            normalizedNext.city,
                            normalizedNext.state
                        ].filter(Boolean).join(', '),
                        demand: normalizedNext.buyerProfile?.preferredCommodities ?? [],
                        capacity: normalizedNext.buyerProfile?.procurementCapacity ?? 'Not provided',
                        type: 'Buyer',
                        verified: true,
                        contact: normalizedNext.userEmail,
                        trades: 0,
                        status: 'Verified',
                        createdAt: normalizedNext.createdAt,
                        updatedAt: timestamp
                    });
                }
            }
            db.notifications.unshift(createNotification({
                userEmail: input.userEmail,
                title: 'AgriTrust verified',
                body: `${roleDisplay(normalizedNext.role)} workspace is now verified by ${input.verifiedByRole} review.`,
                category: 'system',
                actionHref: '/profile'
            }));
            updated = normalizedNext;
        });
        return updated;
    },
    async grantWalletCredit (input) {
        let created = null;
        await updateDb((db)=>{
            const timestamp = nowIso();
            const next = {
                id: id('txn'),
                userEmail: input.userEmail,
                type: input.reason,
                amount: input.amount,
                direction: 'in',
                method: `admin-grant:${input.approvedBy}`,
                status: 'verified',
                createdAt: timestamp
            };
            db.financeTransactions.unshift(next);
            db.notifications.unshift(createNotification({
                userEmail: input.userEmail,
                title: 'Admin wallet grant approved',
                body: `INR ${input.amount.toLocaleString()} was credited after proof verification.`,
                category: 'finance',
                actionHref: '/finance'
            }));
            created = next;
        });
        if (!created) throw new Error('Wallet grant failed');
        return created;
    },
    async listCommunicationGroups (userEmail) {
        const db = await readDb();
        const items = userEmail ? db.communicationGroups.filter((group)=>group.memberEmails.some((memberEmail)=>memberEmail.toLowerCase() === userEmail.toLowerCase())) : db.communicationGroups;
        return [
            ...items
        ].sort((a, b)=>b.updatedAt.localeCompare(a.updatedAt));
    },
    async createCommunicationGroup (input) {
        let created = null;
        await updateDb((db)=>{
            const timestamp = nowIso();
            const next = {
                id: id('grp'),
                ...input,
                status: 'active',
                createdAt: timestamp,
                updatedAt: timestamp
            };
            db.communicationGroups.unshift(next);
            for (const email of next.memberEmails){
                db.notifications.unshift(createNotification({
                    userEmail: email,
                    title: 'Added to communication group',
                    body: `You were added to ${next.name}.`,
                    category: 'community',
                    actionHref: '/community'
                }));
            }
            created = next;
        });
        if (!created) throw new Error('Communication group creation failed');
        return created;
    },
    async listCommunicationMessages (groupId) {
        const db = await readDb();
        const items = groupId ? db.communicationGroupMessages.filter((message)=>message.groupId === groupId) : db.communicationGroupMessages;
        return [
            ...items
        ].sort((a, b)=>a.createdAt.localeCompare(b.createdAt));
    },
    async createCommunicationMessage (input) {
        let created = null;
        await updateDb((db)=>{
            const group = db.communicationGroups.find((item)=>item.id === input.groupId);
            if (!group) {
                throw new Error('Communication group not found.');
            }
            const next = {
                id: id('msg'),
                ...input,
                createdAt: nowIso()
            };
            db.communicationGroupMessages.push(next);
            group.updatedAt = next.createdAt;
            for (const email of group.memberEmails.filter((memberEmail)=>memberEmail.toLowerCase() !== input.senderEmail.toLowerCase())){
                db.notifications.unshift(createNotification({
                    userEmail: email,
                    title: `New message in ${group.name}`,
                    body: `${input.senderName}: ${input.body.slice(0, 80)}`,
                    category: 'community',
                    actionHref: '/community'
                }));
            }
            created = next;
        });
        if (!created) throw new Error('Communication message creation failed');
        return created;
    },
    async getUserWorkspace (email) {
        const db = await readDb();
        const normalizedEmail = normalizeEmail(email);
        const user = db.users.find((entry)=>normalizeEmail(entry.email) === normalizedEmail);
        if (!user) return null;
        const existing = db.profiles.find((profile)=>normalizeEmail(profile.userEmail) === normalizedEmail);
        return normalizeWorkspaceProfileForRole(existing ?? createWorkspaceProfileForUser(toSafeUser(user)));
    },
    async upsertUserWorkspace (email, updates) {
        let updated = null;
        await updateDb((db)=>{
            const normalizedEmail = normalizeEmail(email);
            const user = db.users.find((entry)=>normalizeEmail(entry.email) === normalizedEmail);
            if (!user) return;
            ensureWorkspaceProfile(db, toSafeUser(user));
            const index = db.profiles.findIndex((profile)=>normalizeEmail(profile.userEmail) === normalizedEmail);
            if (index < 0) return;
            const current = db.profiles[index];
            let next = {
                ...current,
                ...updates,
                farmerProfile: updates.farmerProfile ? {
                    ...current.farmerProfile,
                    ...updates.farmerProfile
                } : current.farmerProfile,
                buyerProfile: updates.buyerProfile ? {
                    ...current.buyerProfile,
                    ...updates.buyerProfile
                } : current.buyerProfile,
                adminProfile: updates.adminProfile ? {
                    ...current.adminProfile,
                    ...updates.adminProfile
                } : current.adminProfile,
                notificationPreferences: {
                    ...current.notificationPreferences,
                    ...updates.notificationPreferences ?? {}
                },
                verification: {
                    farmer: updates.verification?.farmer ?? current.verification.farmer,
                    buyer: updates.verification?.buyer ?? current.verification.buyer,
                    admin: updates.verification?.admin ?? current.verification.admin
                },
                updatedAt: nowIso()
            };
            next = normalizeWorkspaceProfileForRole(next);
            db.profiles[index] = next;
            updated = next;
            if (user.role === 'buyer') {
                const buyerIndex = db.buyers.findIndex((buyer)=>normalizeEmail(buyer.contact ?? '') === normalizedEmail);
                if (buyerIndex >= 0) {
                    db.buyers[buyerIndex] = {
                        ...db.buyers[buyerIndex],
                        name: next.buyerProfile?.businessName || next.displayName,
                        location: [
                            next.city,
                            next.state
                        ].filter(Boolean).join(', '),
                        verified: Boolean(next.verification.buyer?.status === 'verified'),
                        contact: normalizedEmail,
                        updatedAt: next.updatedAt
                    };
                } else {
                    db.buyers.unshift({
                        id: id('buy'),
                        name: next.buyerProfile?.businessName || next.displayName,
                        location: [
                            next.city,
                            next.state
                        ].filter(Boolean).join(', '),
                        demand: next.buyerProfile?.preferredCommodities ?? [],
                        capacity: next.buyerProfile?.procurementCapacity ?? 'Not provided',
                        type: 'Buyer',
                        verified: Boolean(next.verification.buyer?.status === 'verified'),
                        contact: normalizedEmail,
                        trades: 0,
                        status: next.verification.buyer?.status === 'verified' ? 'Verified' : 'Pending',
                        createdAt: next.createdAt,
                        updatedAt: next.updatedAt
                    });
                }
            }
        });
        return updated;
    },
    async listNotifications (userEmail) {
        const db = await readDb();
        const notifications = userEmail ? db.notifications.filter((notification)=>notification.userEmail.toLowerCase() === userEmail.toLowerCase()) : db.notifications;
        return [
            ...notifications
        ].sort((a, b)=>b.createdAt.localeCompare(a.createdAt));
    },
    async createNotificationForUser (input) {
        let created = null;
        await updateDb((db)=>{
            const next = createNotification(input);
            db.notifications.unshift(next);
            created = next;
        });
        if (!created) throw new Error('Notification creation failed');
        return created;
    },
    async markNotificationRead (idValue) {
        let updated = null;
        await updateDb((db)=>{
            const index = db.notifications.findIndex((notification)=>notification.id === idValue);
            if (index < 0) return;
            db.notifications[index] = {
                ...db.notifications[index],
                read: true
            };
            updated = db.notifications[index];
        });
        return updated;
    },
    async searchGlobal (query, userEmail) {
        const db = await readDb();
        const value = query.trim().toLowerCase();
        if (!value) return [];
        const routeMatches = [
            {
                id: 'route-dashboard',
                type: 'route',
                title: 'Dashboard',
                subtitle: 'Open operations dashboard',
                href: '/'
            },
            {
                id: 'route-marketplace',
                type: 'route',
                title: 'Marketplace',
                subtitle: 'Browse inputs and farmer lots',
                href: '/marketplace'
            },
            {
                id: 'route-buyers',
                type: 'route',
                title: 'Buyer network',
                subtitle: 'Open verified buyer directory',
                href: '/buyers'
            },
            {
                id: 'route-profile',
                type: 'route',
                title: 'My profile',
                subtitle: 'Manage Agri Trust Protocol data',
                href: '/profile'
            },
            {
                id: 'route-analytics',
                type: 'route',
                title: 'Analytics',
                subtitle: 'Open reporting workspace',
                href: '/analytics'
            },
            {
                id: 'route-developer',
                type: 'route',
                title: 'About site developer',
                subtitle: 'View developer details and profile',
                href: '/about-developer'
            }
        ].filter((item)=>`${item.title} ${item.subtitle}`.toLowerCase().includes(value));
        const results = [
            ...routeMatches,
            ...db.products.filter((product)=>`${product.name} ${product.category} ${product.location ?? ''}`.toLowerCase().includes(value)).slice(0, 5).map((product)=>({
                    id: product.id,
                    type: 'product',
                    title: product.name,
                    subtitle: `${product.category} · ${product.location ?? 'Marketplace'} · INR ${product.price.toLocaleString()}`,
                    href: '/marketplace'
                })),
            ...db.listings.filter((listing)=>`${listing.name} ${listing.commodity ?? ''} ${listing.location}`.toLowerCase().includes(value)).slice(0, 5).map((listing)=>({
                    id: listing.id,
                    type: 'listing',
                    title: listing.name,
                    subtitle: `${listing.location} · ${listing.quantity ?? 0} MT · INR ${listing.price.toLocaleString()}`,
                    href: '/marketplace'
                })),
            ...db.buyers.filter((buyer)=>`${buyer.name} ${buyer.location} ${buyer.demand.join(' ')}`.toLowerCase().includes(value)).slice(0, 5).map((buyer)=>({
                    id: buyer.id,
                    type: 'buyer',
                    title: buyer.name,
                    subtitle: `${buyer.location} · ${buyer.capacity}`,
                    href: '/buyers'
                })),
            ...db.orders.filter((order)=>{
                if (userEmail && order.userEmail.toLowerCase() !== userEmail.toLowerCase()) return false;
                return `${order.id} ${order.userName}`.toLowerCase().includes(value);
            }).slice(0, 5).map((order)=>({
                    id: order.id,
                    type: 'order',
                    title: order.id,
                    subtitle: `${order.userName} · INR ${order.totalAmount.toLocaleString()} · ${order.status}`,
                    href: '/profile'
                })),
            ...db.notifications.filter((notification)=>{
                if (userEmail && notification.userEmail.toLowerCase() !== userEmail.toLowerCase()) return false;
                return `${notification.title} ${notification.body}`.toLowerCase().includes(value);
            }).slice(0, 5).map((notification)=>({
                    id: notification.id,
                    type: 'notification',
                    title: notification.title,
                    subtitle: notification.body,
                    href: notification.actionHref || '/profile'
                }))
        ];
        return results.slice(0, 12);
    },
    async getConfig () {
        const db = await readDb();
        return db.config;
    },
    async updateConfig (updates) {
        let updated = null;
        await updateDb((db)=>{
            db.config = {
                accessPolicy: {
                    ...db.config.accessPolicy,
                    ...updates.accessPolicy ?? {}
                },
                categories: {
                    crops: updates.categories?.crops ?? db.config.categories.crops,
                    inputs: updates.categories?.inputs ?? db.config.categories.inputs
                },
                ui: {
                    supportedLanguages: updates.ui?.supportedLanguages ?? db.config.ui.supportedLanguages,
                    defaultLanguage: updates.ui?.defaultLanguage ?? db.config.ui.defaultLanguage
                },
                finance: {
                    settlementAccountName: updates.finance?.settlementAccountName ?? db.config.finance.settlementAccountName,
                    settlementAccountNumber: updates.finance?.settlementAccountNumber ?? db.config.finance.settlementAccountNumber,
                    settlementIfsc: updates.finance?.settlementIfsc ?? db.config.finance.settlementIfsc,
                    settlementUpi: updates.finance?.settlementUpi ?? db.config.finance.settlementUpi
                },
                developer: {
                    name: updates.developer?.name ?? db.config.developer.name,
                    photoUrl: updates.developer?.photoUrl ?? db.config.developer.photoUrl,
                    college: updates.developer?.college ?? db.config.developer.college,
                    rollNo: updates.developer?.rollNo ?? db.config.developer.rollNo,
                    address: updates.developer?.address ?? db.config.developer.address,
                    contact: updates.developer?.contact ?? db.config.developer.contact,
                    bio: updates.developer?.bio ?? db.config.developer.bio
                }
            };
            updated = db.config;
        });
        if (!updated) throw new Error('Config update failed');
        return updated;
    },
    async createAdminUser (name, email) {
        const temporaryPassword = `Admin@${Math.floor(1000 + Math.random() * 9000)}`;
        let created = null;
        await updateDb((db)=>{
            if (db.users.some((u)=>u.email.toLowerCase() === email.toLowerCase())) {
                throw new Error('User with this email already exists.');
            }
            const timestamp = nowIso();
            const next = {
                id: id('usr'),
                name,
                email,
                passwordHash: hashPassword(temporaryPassword),
                role: 'admin',
                status: 'active',
                isPro: true,
                aiCredits: 9999,
                createdAt: timestamp,
                updatedAt: timestamp
            };
            db.users.push(next);
            created = toSafeUser(next);
            if (created) {
                ensureWorkspaceProfile(db, created);
            }
        });
        if (!created) throw new Error('Admin creation failed');
        return {
            user: created,
            temporaryPassword
        };
    }
};

})()),
"[project]/src/lib/server/upstream.ts [app-rsc] (ecmascript)": (({ r: __turbopack_require__, f: __turbopack_module_context__, i: __turbopack_import__, s: __turbopack_esm__, v: __turbopack_export_value__, n: __turbopack_export_namespace__, c: __turbopack_cache__, M: __turbopack_modules__, l: __turbopack_load__, j: __turbopack_dynamic__, P: __turbopack_resolve_absolute_path__, U: __turbopack_relative_url__, R: __turbopack_resolve_module_id_path__, g: global, __dirname, x: __turbopack_external_require__, y: __turbopack_external_import__ }) => (() => {
"use strict";

__turbopack_esm__({
    "backendAccessCookieName": ()=>backendAccessCookieName,
    "backendRefreshCookieName": ()=>backendRefreshCookieName,
    "clearBackendAuthCookies": ()=>clearBackendAuthCookies,
    "fetchAi": ()=>fetchAi,
    "fetchBackend": ()=>fetchBackend,
    "hasAiUpstream": ()=>hasAiUpstream,
    "hasBackendUpstream": ()=>hasBackendUpstream,
    "mapRoleFromBackend": ()=>mapRoleFromBackend,
    "mapRoleToBackend": ()=>mapRoleToBackend,
    "normalizeBackendPrices": ()=>normalizeBackendPrices,
    "normalizeBackendUser": ()=>normalizeBackendUser,
    "setBackendAuthCookiesFromResponse": ()=>setBackendAuthCookiesFromResponse
});
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$headers$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__ = __turbopack_import__("[project]/node_modules/next/headers.js [app-rsc] (ecmascript)");
"__TURBOPACK__ecmascript__hoisting__location__";
;
const BACKEND_API_URL = process.env.BACKEND_API_URL?.replace(/\/$/, '') || '';
const AI_SERVICE_URL = process.env.AI_SERVICE_URL?.replace(/\/$/, '') || '';
const backendAccessCookieName = 'backend_access_token';
const backendRefreshCookieName = 'backend_refresh_token';
function hasBackendUpstream() {
    return Boolean(BACKEND_API_URL);
}
function hasAiUpstream() {
    return Boolean(AI_SERVICE_URL);
}
function mapRoleFromBackend(role) {
    if (role === 'buyer' || role === 'admin') return role;
    return 'user';
}
function mapRoleToBackend(role) {
    if (role === 'buyer' || role === 'admin') return role;
    return 'farmer';
}
function normalizeBackendUser(user) {
    const timestamp = new Date().toISOString();
    return {
        id: user.id || user._id || 'remote-user',
        name: user.name || 'User',
        email: user.email || '',
        role: mapRoleFromBackend(user.role),
        status: 'active',
        isPro: true,
        aiCredits: 9999,
        createdAt: timestamp,
        updatedAt: timestamp
    };
}
function parseSetCookieValue(setCookie, cookieName) {
    if (!setCookie) return null;
    const escaped = cookieName.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
    const match = setCookie.match(new RegExp(`${escaped}=([^;]+)`));
    return match?.[1] ?? null;
}
async function setBackendAuthCookiesFromResponse(response, accessToken) {
    const cookieStore = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$headers$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["cookies"])();
    const setCookies = typeof response.headers.getSetCookie === 'function' ? response.headers.getSetCookie() : [];
    const joined = setCookies.join('; ');
    const refreshToken = parseSetCookieValue(joined, 'refreshToken');
    const upstreamAccessToken = accessToken || parseSetCookieValue(joined, 'accessToken');
    if (upstreamAccessToken) {
        cookieStore.set(backendAccessCookieName, upstreamAccessToken, {
            httpOnly: true,
            sameSite: 'lax',
            secure: ("TURBOPACK compile-time value", "development") === 'production',
            path: '/',
            maxAge: 60 * 15
        });
    }
    if (refreshToken) {
        cookieStore.set(backendRefreshCookieName, refreshToken, {
            httpOnly: true,
            sameSite: 'lax',
            secure: ("TURBOPACK compile-time value", "development") === 'production',
            path: '/',
            maxAge: 60 * 60 * 24 * 7
        });
    }
}
function clearBackendAuthCookies() {
    const cookieStore = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$headers$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["cookies"])();
    for (const name of [
        backendAccessCookieName,
        backendRefreshCookieName
    ]){
        cookieStore.set(name, '', {
            httpOnly: true,
            sameSite: 'lax',
            secure: ("TURBOPACK compile-time value", "development") === 'production',
            path: '/',
            maxAge: 0
        });
    }
}
async function fetchBackend(path, init) {
    const accessToken = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$headers$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["cookies"])().get(backendAccessCookieName)?.value;
    const headers = new Headers(init?.headers);
    if (!headers.has('Content-Type') && init?.body) headers.set('Content-Type', 'application/json');
    if (accessToken) headers.set('Authorization', `Bearer ${accessToken}`);
    return fetch(`${BACKEND_API_URL}${path}`, {
        ...init,
        headers,
        cache: 'no-store'
    });
}
async function fetchAi(path, body) {
    if (!hasAiUpstream()) return null;
    const response = await fetch(`${AI_SERVICE_URL}${path}`, {
        method: 'POST',
        headers: {
            'Content-Type': 'application/json'
        },
        body: JSON.stringify(body),
        cache: 'no-store'
    });
    if (!response.ok) return null;
    return await response.json();
}
function normalizeBackendPrices(prices) {
    return prices.map((price, index)=>{
        const value = Number(price.currentPrice ?? price.price ?? 0);
        const previous = Number(price.previousPrice ?? value);
        const changePct = previous ? Number(((value - previous) / previous * 100).toFixed(2)) : 0;
        return {
            id: String(price._id ?? price.id ?? `remote-price-${index}`),
            commodity: String(price.commodity ?? 'Commodity'),
            market: String(price.market ?? 'Market'),
            price: value,
            unit: 'INR/quintal',
            changePct,
            trend: changePct > 0 ? 'up' : changePct < 0 ? 'down' : 'stable',
            timestamp: String(price.updatedAt ?? price.createdAt ?? new Date().toISOString())
        };
    });
}

})()),
"[project]/src/lib/server/auth.ts [app-rsc] (ecmascript)": (({ r: __turbopack_require__, f: __turbopack_module_context__, i: __turbopack_import__, s: __turbopack_esm__, v: __turbopack_export_value__, n: __turbopack_export_namespace__, c: __turbopack_cache__, M: __turbopack_modules__, l: __turbopack_load__, j: __turbopack_dynamic__, P: __turbopack_resolve_absolute_path__, U: __turbopack_relative_url__, R: __turbopack_resolve_module_id_path__, g: global, __dirname, x: __turbopack_external_require__, y: __turbopack_external_import__ }) => (() => {
"use strict";

__turbopack_esm__({
    "clearLocalSessionCookie": ()=>clearLocalSessionCookie,
    "getSessionUser": ()=>getSessionUser,
    "requireSessionUser": ()=>requireSessionUser,
    "setLocalSessionCookie": ()=>setLocalSessionCookie
});
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$headers$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__ = __turbopack_import__("[project]/node_modules/next/headers.js [app-rsc] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$server$2f$store$2e$ts__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__ = __turbopack_import__("[project]/src/lib/server/store.ts [app-rsc] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$server$2f$upstream$2e$ts__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__ = __turbopack_import__("[project]/src/lib/server/upstream.ts [app-rsc] (ecmascript)");
"__TURBOPACK__ecmascript__hoisting__location__";
;
;
;
function setLocalSessionCookie(user) {
    const token = __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$server$2f$store$2e$ts__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["platformStore"].createSessionToken(user);
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$headers$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["cookies"])().set(__TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$server$2f$store$2e$ts__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["platformStore"].sessionCookieName, token, {
        httpOnly: true,
        sameSite: 'lax',
        secure: ("TURBOPACK compile-time value", "development") === 'production',
        path: '/',
        maxAge: 60 * 60 * 24 * 7
    });
}
function clearLocalSessionCookie() {
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$headers$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["cookies"])().set(__TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$server$2f$store$2e$ts__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["platformStore"].sessionCookieName, '', {
        httpOnly: true,
        sameSite: 'lax',
        secure: ("TURBOPACK compile-time value", "development") === 'production',
        path: '/',
        maxAge: 0
    });
}
async function getSessionUser() {
    if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$server$2f$upstream$2e$ts__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["hasBackendUpstream"])()) {
        const backendToken = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$headers$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["cookies"])().get(__TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$server$2f$upstream$2e$ts__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["backendAccessCookieName"])?.value;
        if (backendToken) {
            try {
                const response = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$server$2f$upstream$2e$ts__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["fetchBackend"])('/api/auth/me');
                if (response.ok) {
                    const payload = await response.json();
                    if (payload.data?.user) {
                        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$server$2f$upstream$2e$ts__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["normalizeBackendUser"])(payload.data.user);
                    }
                }
            } catch  {
            // Fall back to local session if upstream auth is unavailable.
            }
        }
    }
    const token = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$headers$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["cookies"])().get(__TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$server$2f$store$2e$ts__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["platformStore"].sessionCookieName)?.value;
    if (!token) return null;
    const session = __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$server$2f$store$2e$ts__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["platformStore"].verifySessionToken(token);
    if (!session) return null;
    return __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$server$2f$store$2e$ts__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["platformStore"].findUserByEmail(session.email);
}
async function requireSessionUser() {
    const user = await getSessionUser();
    if (!user) {
        throw new Error('Authentication required');
    }
    return user;
}

})()),
"[project]/src/app/layout.tsx [app-rsc] (ecmascript)": (({ r: __turbopack_require__, f: __turbopack_module_context__, i: __turbopack_import__, s: __turbopack_esm__, v: __turbopack_export_value__, n: __turbopack_export_namespace__, c: __turbopack_cache__, M: __turbopack_modules__, l: __turbopack_load__, j: __turbopack_dynamic__, P: __turbopack_resolve_absolute_path__, U: __turbopack_relative_url__, R: __turbopack_resolve_module_id_path__, g: global, __dirname, x: __turbopack_external_require__, y: __turbopack_external_import__ }) => (() => {
"use strict";

__turbopack_esm__({
    "default": ()=>RootLayout,
    "metadata": ()=>metadata
});
var __TURBOPACK__imported__module__$5b$next$5d2f$internal$2f$font$2f$google$2f$sora_177802fc$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__ = __turbopack_import__("[next]/internal/font/google/sora_177802fc.js [app-rsc] (ecmascript)");
var __TURBOPACK__imported__module__$5b$next$5d2f$internal$2f$font$2f$google$2f$merriweather_ee88fc3$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__ = __turbopack_import__("[next]/internal/font/google/merriweather_ee88fc3.js [app-rsc] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$server$2f$future$2f$route$2d$modules$2f$app$2d$page$2f$vendored$2f$rsc$2f$react$2d$jsx$2d$dev$2d$runtime$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__ = __turbopack_import__("[project]/node_modules/next/dist/server/future/route-modules/app-page/vendored/rsc/react-jsx-dev-runtime.js [app-rsc] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$components$2f$ui$2f$toaster$2e$tsx__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__ = __turbopack_import__("[project]/src/components/ui/toaster.tsx [app-rsc] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$context$2f$auth$2d$context$2e$tsx__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__ = __turbopack_import__("[project]/src/context/auth-context.tsx [app-rsc] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$context$2f$language$2d$context$2e$tsx__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__ = __turbopack_import__("[project]/src/context/language-context.tsx [app-rsc] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$server$2f$auth$2e$ts__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__ = __turbopack_import__("[project]/src/lib/server/auth.ts [app-rsc] (ecmascript)");
"__TURBOPACK__ecmascript__hoisting__location__";
;
;
;
;
;
;
;
;
const metadata = {
    title: "Farmer's Marketplace - Direct Market Access Web Application",
    description: "Farmer's Marketplace - Direct Market Access Web Application for farmers, buyers, and administrators."
};
async function RootLayout({ children }) {
    const initialUser = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$server$2f$auth$2e$ts__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["getSessionUser"])();
    return /*#__PURE__*/ (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$server$2f$future$2f$route$2d$modules$2f$app$2d$page$2f$vendored$2f$rsc$2f$react$2d$jsx$2d$dev$2d$runtime$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["jsxDEV"])("html", {
        lang: "en",
        suppressHydrationWarning: true,
        className: `${__TURBOPACK__imported__module__$5b$next$5d2f$internal$2f$font$2f$google$2f$sora_177802fc$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["default"].variable} ${__TURBOPACK__imported__module__$5b$next$5d2f$internal$2f$font$2f$google$2f$merriweather_ee88fc3$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["default"].variable}`,
        children: [
            /*#__PURE__*/ (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$server$2f$future$2f$route$2d$modules$2f$app$2d$page$2f$vendored$2f$rsc$2f$react$2d$jsx$2d$dev$2d$runtime$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["jsxDEV"])("head", {}, void 0, false, {
                fileName: "[project]/src/app/layout.tsx",
                lineNumber: 26,
                columnNumber: 7
            }, this),
            /*#__PURE__*/ (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$server$2f$future$2f$route$2d$modules$2f$app$2d$page$2f$vendored$2f$rsc$2f$react$2d$jsx$2d$dev$2d$runtime$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["jsxDEV"])("body", {
                className: "font-body antialiased",
                suppressHydrationWarning: true,
                children: /*#__PURE__*/ (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$server$2f$future$2f$route$2d$modules$2f$app$2d$page$2f$vendored$2f$rsc$2f$react$2d$jsx$2d$dev$2d$runtime$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["jsxDEV"])(__TURBOPACK__imported__module__$5b$project$5d2f$src$2f$context$2f$language$2d$context$2e$tsx__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["LanguageProvider"], {
                    children: /*#__PURE__*/ (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$server$2f$future$2f$route$2d$modules$2f$app$2d$page$2f$vendored$2f$rsc$2f$react$2d$jsx$2d$dev$2d$runtime$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["jsxDEV"])(__TURBOPACK__imported__module__$5b$project$5d2f$src$2f$context$2f$auth$2d$context$2e$tsx__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["AuthProvider"], {
                        initialUser: initialUser,
                        sessionResolved: true,
                        children: [
                            children,
                            /*#__PURE__*/ (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$server$2f$future$2f$route$2d$modules$2f$app$2d$page$2f$vendored$2f$rsc$2f$react$2d$jsx$2d$dev$2d$runtime$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["jsxDEV"])(__TURBOPACK__imported__module__$5b$project$5d2f$src$2f$components$2f$ui$2f$toaster$2e$tsx__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["Toaster"], {}, void 0, false, {
                                fileName: "[project]/src/app/layout.tsx",
                                lineNumber: 31,
                                columnNumber: 15
                            }, this)
                        ]
                    }, void 0, true, {
                        fileName: "[project]/src/app/layout.tsx",
                        lineNumber: 29,
                        columnNumber: 11
                    }, this)
                }, void 0, false, {
                    fileName: "[project]/src/app/layout.tsx",
                    lineNumber: 28,
                    columnNumber: 9
                }, this)
            }, void 0, false, {
                fileName: "[project]/src/app/layout.tsx",
                lineNumber: 27,
                columnNumber: 7
            }, this)
        ]
    }, void 0, true, {
        fileName: "[project]/src/app/layout.tsx",
        lineNumber: 25,
        columnNumber: 5
    }, this);
}

})()),
"[project]/src/app/layout.tsx [app-rsc] (ecmascript, Next.js server component)": (({ r: __turbopack_require__, f: __turbopack_module_context__, i: __turbopack_import__, s: __turbopack_esm__, v: __turbopack_export_value__, n: __turbopack_export_namespace__, c: __turbopack_cache__, M: __turbopack_modules__, l: __turbopack_load__, j: __turbopack_dynamic__, P: __turbopack_resolve_absolute_path__, U: __turbopack_relative_url__, R: __turbopack_resolve_module_id_path__, g: global, __dirname }) => (() => {

__turbopack_esm__({
    default: () => __turbopack_import__("[project]/src/app/layout.tsx [app-rsc] (ecmascript)"),
});

})()),
"[project]/src/app/login/page.tsx (client proxy)": (({ r: __turbopack_require__, f: __turbopack_module_context__, i: __turbopack_import__, s: __turbopack_esm__, v: __turbopack_export_value__, n: __turbopack_export_namespace__, c: __turbopack_cache__, M: __turbopack_modules__, l: __turbopack_load__, j: __turbopack_dynamic__, P: __turbopack_resolve_absolute_path__, U: __turbopack_relative_url__, R: __turbopack_resolve_module_id_path__, g: global, __dirname, x: __turbopack_external_require__, y: __turbopack_external_import__ }) => (() => {
"use strict";

__turbopack_esm__({
    "default": ()=>__TURBOPACK__default__export__
});
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$server$2f$future$2f$route$2d$modules$2f$app$2d$page$2f$vendored$2f$rsc$2f$react$2d$server$2d$dom$2d$turbopack$2d$server$2d$edge$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__ = __turbopack_import__("[project]/node_modules/next/dist/server/future/route-modules/app-page/vendored/rsc/react-server-dom-turbopack-server-edge.js [app-rsc] (ecmascript)");
"__TURBOPACK__ecmascript__hoisting__location__";
;
const __TURBOPACK__default__export__ = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$server$2f$future$2f$route$2d$modules$2f$app$2d$page$2f$vendored$2f$rsc$2f$react$2d$server$2d$dom$2d$turbopack$2d$server$2d$edge$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["registerClientReference"])(function() {
    throw new Error("Attempted to call the default export of [project]/src/app/login/page.tsx from the server, but it's on the client. It's not possible to invoke a client function from the server, it can only be rendered as a Component or passed to props of a Client Component.");
}, "[project]/src/app/login/page.tsx", "default");

})()),
"[project]/src/app/login/page.tsx [app-rsc] (ecmascript)": (({ r: __turbopack_require__, f: __turbopack_module_context__, i: __turbopack_import__, s: __turbopack_esm__, v: __turbopack_export_value__, n: __turbopack_export_namespace__, c: __turbopack_cache__, M: __turbopack_modules__, l: __turbopack_load__, j: __turbopack_dynamic__, P: __turbopack_resolve_absolute_path__, U: __turbopack_relative_url__, R: __turbopack_resolve_module_id_path__, g: global, __dirname, x: __turbopack_external_require__, y: __turbopack_external_import__ }) => (() => {
"use strict";

var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$app$2f$login$2f$page$2e$tsx__$28$client__proxy$29$__ = __turbopack_import__("[project]/src/app/login/page.tsx (client proxy)");
"__TURBOPACK__ecmascript__hoisting__location__";
"TURBOPACK { transition: next-ecmascript-client-reference }";
;
__turbopack_export_namespace__(__TURBOPACK__imported__module__$5b$project$5d2f$src$2f$app$2f$login$2f$page$2e$tsx__$28$client__proxy$29$__);

})()),
"[project]/src/app/login/page.tsx [app-rsc] (ecmascript, Next.js server component)": (({ r: __turbopack_require__, f: __turbopack_module_context__, i: __turbopack_import__, s: __turbopack_esm__, v: __turbopack_export_value__, n: __turbopack_export_namespace__, c: __turbopack_cache__, M: __turbopack_modules__, l: __turbopack_load__, j: __turbopack_dynamic__, P: __turbopack_resolve_absolute_path__, U: __turbopack_relative_url__, R: __turbopack_resolve_module_id_path__, g: global, __dirname }) => (() => {

__turbopack_esm__({
    default: () => __turbopack_import__("[project]/src/app/login/page.tsx [app-rsc] (ecmascript)"),
});

})()),
"[project]/.next-internal/server/app/login/page/actions.js [app-rsc] (ecmascript)": (function({ r: __turbopack_require__, f: __turbopack_module_context__, i: __turbopack_import__, s: __turbopack_esm__, v: __turbopack_export_value__, n: __turbopack_export_namespace__, c: __turbopack_cache__, M: __turbopack_modules__, l: __turbopack_load__, j: __turbopack_dynamic__, P: __turbopack_resolve_absolute_path__, U: __turbopack_relative_url__, R: __turbopack_resolve_module_id_path__, g: global, __dirname, x: __turbopack_external_require__, y: __turbopack_external_import__, m: module, e: exports, t: require }) { !function() {

__turbopack_export_value__({});

}.call(this) }),

};

//# sourceMappingURL=%5Broot%20of%20the%20server%5D__5a3d1f._.js.map